# Non-secret POC-01 gateway addressing/network config. Sourced by provision.sh
# and the scripts/ tools. Keep addressing here, not scattered as literals.

AWG_SUBNET_CIDR=10.77.0.0/24
GATEWAY_TUNNEL_IP=10.77.0.1
GATEWAY_TUNNEL_PREFIX=24
LISTEN_PORT=51820
INTERFACE_NAME=awg0
CONFIG_DIR=/etc/amnezia/amneziawg
CONFIG_FILE=awg0.conf
SERVICE_NAME=awg-poc
NFT_TABLE=pocvpn
BUILD_DIR=/opt/pocvpn/build
