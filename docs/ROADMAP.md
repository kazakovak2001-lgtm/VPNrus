# Product roadmap - approved capabilities and current status

This document records the approved long-term capability set for the VPN
product. **It is documentation only.** Nothing in this file implements,
enables, or wires up any of the PLANNED/BLOCKED items - see each item's
status for what actually exists in the repository today, and see
`docs/AWG_GENERATION.md` / `gateway/README.md` and the later evidence below
for capabilities already exercised on real gateways and Android devices.

## Status legend

| Status | Meaning |
|---|---|
| **IMPLEMENTED** | Real, working code exists and does what the name says. |
| **FOUNDATION** | Real, tested code exists (types, pure decision logic, an observer), but it is either not wired into the live product flow, or only covers part of the eventual capability. |
| **PLANNED** | Approved for the roadmap. No code exists yet. |
| **BLOCKED** | Approved, but cannot start/complete because of a stated external precondition. |
| **UNVERIFIED** | Not a feature - a *claim* about the running system (e.g. "no DNS leak") that has not yet been proven with real evidence, and must not be asserted as true until it is. |

A capability's status here is not a promise about when it ships - it is a
truthful snapshot, updated as each gate (B6, B8B, B9, B10, and beyond)
actually lands.

## Mandatory runtime sequence (unchanged)

```
B6  - real VPS provisioning
B8B - public AWG handshake
B9  - full-tunnel external IP validation
B10 - DNS / IPv4 / IPv6 leak validation
```

The original sequencing rule deferred new transports until B10 completed on
a real VPS. XRay/REALITY was the first historical exception: its full
client and gateway runtime was built and live-wired ahead of B10, and its
AWG->Xray automatic failover has since been **verified** end-to-end on a
real VPS (see the XRay / VLESS REALITY fallback row below). Phase 2A
provided architecture foundation ahead of that sequence; the later B10
completion removed this process gate for subsequent transport work.

**As of B10-1 (2026-08-30), the B6->B8B->B9->B10 sequence above is
itself complete** - see the "Current verification status" table below:
public AWG handshake, public VPS NAT behavior, external IP change, and
DNS/IPv4/IPv6 leak protection are all now VERIFIED with real physical-
device evidence (B6, real VPS provisioning, has been implicitly true
throughout this document's B8I/B8K/B8L/B8M/B8N history - the gateway at
`152.70.43.1` this evidence was gathered against is that same real VPS).
This means the sequencing GATE itself - not merely an exception granted
to one transport - no longer blocks QUIC/TLS/TCP-fallback/Shadowsocks
runtime work on process grounds; any decision to start one is now a
scope/priority choice, not a blocked one. `Real restrictive-network/
Russia behavior` (the verification table's final row) remains
**UNVERIFIED and separate** - B10's leak validation says nothing about
behavior on an actually-restrictive network, and must not be conflated
with it.

## Architecture principles this roadmap commits to

1. **Routing decision and transport selection stay separate.** "Should this
   traffic use the VPN at all" (`RoutingDecisionEngine`) is never the same
   question as "which transport carries it" (`SmartConnectDecisionEngine`).
   Do not merge these two concerns as the roadmap items below are built.

2. **Target Smart Connect pipeline:**

   ```
   NetworkProfiler -> RestrictionClassifier -> RoutingDecisionEngine
   ReachabilityEngine -> PathCandidateBuilder -> PathScorer
     -> AutoGatewaySelector (automatic gateway mode)
     -> TransportOrchestrator -> VpnController
   ```

   The B11 observational-only description was a historical baseline,
   superseded by B16/B19: `AutoGatewaySelector` now consumes the existing
   reachability/scoring fabric in automatic mode, selects real gateway and
   transport candidates, and has physical Android gateway-selection evidence.
   Manual mode retains its separate `SmartConnectDecisionEngine` transport
   picker. `RoutingDecisionEngine` is the live DIRECT-vs-VPN route authority
   (B18); it does not choose a gateway. Restriction evidence is consumed
   conservatively by routing and automatic candidate ranking (B18/B28),
   without country or provider-name rules. The physical B16 pass excluded an
   unprovisioned gateway; same-request mid-connect timeout failover remains
   unit-tested, as documented in its row below.

3. **Distinct failure conditions.** A connection attempt that fails must be
   attributable to one of (at minimum):

   - `PROTOCOL_BLOCKED` - the transport protocol itself appears filtered.
   - `GATEWAY_UNREACHABLE` - this specific gateway/endpoint is down or
     unreachable, independent of protocol.
   - `NETWORK_UNAVAILABLE` - the device has no usable network at all
     (already modeled: `NetworkProfile.isUsable`,
     `TransportSelectionDecision.NetworkUnavailable`).
   - `HARD_WHITELIST` - the network appears to only permit a fixed set of
     allowlisted destinations (see Hard Whitelist Detection below).

   These must never be collapsed into one generic "connection failed"
   state - each implies a different remediation (retry, switch gateway,
   switch transport, or something structurally different for hard
   whitelists).

4. **Hard Whitelist Detection must never impersonate a real third-party
   service.** Detecting that a network only allows traffic to a fixed
   allowlist (e.g. a bank, Yandex, VK) must not be implemented by
   pretending to *be* one of those services, spoofing their traffic
   signature, or otherwise misrepresenting the client as that service.
   Detection is passive/observational only.

5. **Adaptive Direct Routing** means genuine `DIRECT` routing (the
   underlying network, unmodified) for explicitly allowed/local traffic,
   and VPN routing for everything else classified as needing protection.
   It uses the existing `RoutingDecisionEngine` / `ClientRoutingPolicy`
   model. B18 wired the real `RestrictionClassifier` conservatively into
   route decisions, and later physical validation promoted Adaptive Direct
   Routing to IMPLEMENTED (see its row). This is not a new routing primitive.

6. **Transport selection uses capability/health scoring, not hardcoded
   if/else.** `TransportCapabilities` and `TransportHealth` (Phase 2A,
   FOUNDATION) exist for exactly this reason: as more transports become
   real, `SmartConnectDecisionEngine` must be extended to score across
   these typed fields, never to grow a chain of per-protocol conditionals.

7. **Public gateway infrastructure must span multiple providers/ASNs.**
   Relying on a single provider (e.g. Hetzner alone) for all gateways is
   not the target architecture - see Gateway Pool / multi-provider gateway
   infrastructure below. A real, physically-verified production VPS has
   existed since B10-1/B8O2 (2026-08-30), so this is no longer BLOCKED on
   "zero provisioned gateways" - it is now a scope/priority choice (PLANNED),
   the same reconciliation already applied to the B6->B10 sequencing gate
   above. Germany/Oracle and Stockholm/AWS are both deployed, have distinct
   provider/ASN facts in `EndpointDescriptor`, and have physical AWG,
   REALITY and TLS/TCP evidence (B13-B16). A durable, diversified product
   pool and CDN/control-plane diversity remain future work.

8. **Signed Offline Bootstrap** must let the client retain a
   cryptographically verified last-known-good gateway/control
   configuration for use when the primary control API is unreachable - not
   an unauthenticated cached blob.

9. **Private Gateway Mode is a first-class capability, not a fallback.**
   A user may connect through the managed gateway network *or* their own
   compatible VPS running the same pinned AmneziaWG gateway
   (`gateway/provision.sh`) - both are supported product modes.

10. **Privacy claims stay truthful.** Nothing in this product may claim
    "untraceable," a guaranteed censorship bypass, or a guaranteed
    hard-whitelist bypass. Every capability below is described by what it
    actually does, not by an outcome no software can promise.

## P0 - core anti-censorship

| Capability | Status | Notes |
|---|---|---|
| AmneziaWG 3.1 | **IMPLEMENTED** | Client + gateway both real, pinned `v3.1.20260814`/`v3.1.20260812`. Local Android<->WSL2 handshake **VERIFIED** (B8A); public-network handshake **VERIFIED** (B10-1, 2026-08-30) on a real physical Android device against the production gateway. |
| XRay / VLESS REALITY fallback | **IMPLEMENTED** | Client and gateway both real and live-wired. Client: `XrayProfileStore`/`XrayProfileRepository` persist a provisioned profile, `VlessRealityTransport`/`NovaXrayVpnService`/`XrayCoreController` execute it through `VpnController` (B8I6-B8I7), which registers `TransportKind.XRAY_REALITY` as available whenever a real profile repository is wired - production always does. Gateway: Xray profile provisioning/activation is real (`gateway/api/xray_activation.py`, `xray_provisioning.py`, B8K1-B8K4), with its `/v1/activate`/`/v1/xray-profile` routes now exposed at the nginx edge (B8K5A, PR #10). `AwgXrayFailoverPolicy` (B8I8) automatically falls back a failed AWG attempt to Xray, and the REALITY key validation bug is fixed (same commit, PR #9). AWG->Xray automatic failover **VERIFIED** end-to-end on a real VPS (AWG peer removed, AWG failed, automatic Xray fallback executed, live Xray traffic and a matching exit IP confirmed on the server). **B30B physical-validation fix (2026-09-03)**: real Oppo CPH2173 hardware testing found a genuine, confirmed total connectivity loss (airplane mode + WiFi disabled, ~70s, zero `NetworkAgentInfo`) left a TLS_TCP session showing "Protected" the whole time - root cause: `AndroidReconnectManager` used `registerDefaultNetworkCallback`, which reports THIS APP'S OWN default network - once this app's VpnService establishes its own tun interface, that IS the VPN network itself, not the real underlying WiFi/cellular transport beneath it, so `onLost` essentially never fired and `VpnController.handleNetworkLost()`/`reconnectLoop()` (the EXISTING AWG-proven bounded-retry health authority, not AWG-specific by construction - it triggers on any `TransportState.Connected`) was simply never invoked for the TLS_TCP session. AWG itself was never affected (WireGuard's own UDP send failures detect loss independently of this class). Fixed by switching to `registerNetworkCallback` with an explicit `NET_CAPABILITY_NOT_VPN` request (the documented Android pattern for a VPN app to observe real underlying connectivity), tracking the full set of currently-available matching networks (`NetworkAvailabilitySet`, a small pure/testable helper) so a WiFi<->cellular handover with a usable network throughout never spuriously reports "network lost." No new health system, no second polling loop - purely a same-interface fix to `AndroidReconnectManager`'s network-observation scope; `VpnController`/`ReconnectManager`'s contract/`SupportDiagnosticsRecorder`'s existing generic `sessionHealth` backstop are unchanged and now correctly receive the real signal. |
| QUIC transport/fallback | PLANNED | No code. |
| TLS/TCP fallback | **IMPLEMENTED (manual TLS transport); automatic TLS fallback remains outside this claim** | B8O2 added the separate Xray TLS inbound, shared activation boundary, endpoint-scoped TLS profile storage, and VlessTlsTransport through the existing Xray/TUN service. Later Gateway Pool evidence records physical AWG/REALITY/TLS_TCP validation on both Germany/Oracle and Stockholm/AWS, and the Adaptive Direct Routing physical pass confirms Xray/TLS route consistency. The original B8O2 zero-device-verification statement was historical and is superseded. TLS availability still requires a real provisioned profile; this status does not claim automatic TLS fallback in Manual mode. |
| Smart Connect | **FOUNDATION** | `SmartConnectDecisionEngine` (Phase 2A) is real, pure, and unit-tested, and IS live-wired as of B8I1/B8I8 - `MainViewModel.connect()` calls `smartConnectDecision()` (via `SmartConnectCandidateSelector`) for every real MANUAL-gateway-mode attempt, and `AwgXrayFailoverPolicy` consults its outcome for AWG->Xray fallback. `SmartConnectDecisionEngine.decideAuto` (the single-gateway transport picker this powers) still picks by its own fixed `PREFERRED_ORDER` alone - its own `health` parameter is accepted but genuinely unused in the body, unchanged by B19 (Manual mode is deliberately kept simple/deterministic - see architecture principle 1 and B19's own precedence note below). **B19 (2026-09-01) clarification, not new scope**: this row previously read "gateway selection does not exist yet", which has been stale since B16 - `AutoGatewaySelector` (a SEPARATE, real decision authority for AUTOMATIC gateway mode specifically, see the dedicated row below) has reused this exact `PathScorer`/`ReachabilityEngine` pipeline since B16, and DOES let real health/reachability evidence reorder both gateway AND transport together for that mode. The two decision authorities are deliberately never merged (task requirement 1) - this row's own FOUNDATION status reflects `SmartConnectDecisionEngine`/Manual mode specifically, not automatic gateway selection's own (separately tracked) status. |
| Network Profiler | **FOUNDATION** | `NetworkProfiler` (Phase 2A) uses real `ConnectivityManager` callbacks, instrumented-test-verified, and IS consumed by the live connect path (wired into `MainViewModel`'s `Factory`, feeds `smartConnectDecision()`/`RestrictionClassifier`). Kept at FOUNDATION: it only covers device-level connectivity facts, not the rest of architecture principle 2's target pipeline. |
| Restriction Classifier | **FOUNDATION** | Real NetworkProfiler/connection/reachability evidence feeds the unit-tested RestrictionClassifier. B18 makes its conservative output decision-driving in RoutingDecisionEngine (only NO_NETWORK blocks; possible filtering never broadens DIRECT routing), and B28 uses restriction evidence in automatic direct-vs-relay ranking. It does not identify a censor, provider, country, or proven hard-whitelist bypass; real restricted-network behavior remains UNVERIFIED. |
| Hard Whitelist Detection | **FOUNDATION** | `DiverseReachabilityEvaluator` (B8M) is real and unit-tested: a strict-majority read over several diverse, unrelated, real HTTPS destinations (standard OS/browser connectivity-check endpoints - Google/Apple/Mozilla, each already probed for exactly this purpose by its own platform, so this is an honest reachability check, never an impersonation - architecture principle 4). Live-wired: `RestrictionMonitor` (B8J) probes them on the SAME trigger as the existing gateway probe; `RestrictionClassifier` gained a new `POSSIBLE_HARD_WHITELIST` case (validated internet, the gateway itself confirmed unreachable via BOTH HTTPS and AWG - a confirmed-reachable HTTPS control-plane is treated as positive evidence AGAINST a whitelist claim, not ignored - AND a majority of the diverse set also unreachable). Kept at FOUNDATION, not IMPLEMENTED: this is a narrow, conservative signal ("possible", same discipline as `POSSIBLE_UDP_OR_AWG_FILTERING`), not a confirmed detector. **B18 update**: `POSSIBLE_HARD_WHITELIST` IS now evaluated by `RoutingDecisionEngine.decideAdaptiveRoute` (see the Restriction Classifier row above) - but the evaluation itself proves the negative this row has always promised: it is explicitly, unit-test-provenly (`RoutingDecisionEngineTest`) NEVER allowed to route `PROTECTED` traffic DIRECT (that would BE the forbidden "bypass" - architecture principle 4/10). So "carried through truthfully, never decision-driving" is no longer the accurate description (it IS an input to a live decision now) - the accurate one is "a live input to routing that is proven to never widen DIRECT eligibility, by construction." |
| Adaptive Direct Routing | **IMPLEMENTED** | B18's persisted FULL_VPN/ADAPTIVE/APPS mode uses the existing RoutingDecisionEngine as the single DIRECT-vs-VPN route authority. Default/corrupt state fails safe to FULL_VPN; PROTECTED traffic never becomes DIRECT because of uncertain restriction classes. AWG and Xray REALITY/TLS use the shared IPv4 route resolver; IPv6 stays fail-closed, and app eligibility remains with AppRoutingPolicy. Later physical Android evidence in this roadmap confirms working Full VPN and Adaptive protected traffic, DNS/IPv6 invariants, and Xray/TLS route consistency after the AWG peer was re-provisioned through the real control plane. Earlier FOUNDATION/no-device and temporary AWG receive-counter diagnoses in this row were superseded by that completed physical pass. This does not prove Russia/hard-whitelist bypass or per-hostname routing. |
| Gateway Pool | **FOUNDATION** | **CURRENT STATE (2026-08-31, B15 update - supersedes any earlier claim in this row's own history below that it contradicts)**: (A) Germany (Oracle Cloud, `152.70.43.1`) has a real, stable, physically-verified AWG+REALITY+TLS_TCP data plane AND real self-service control-plane provisioning (`POST /v1/activate`, `ClientTunnelIdentityStore` evidence-based migration) - fully usable end to end. (B) Stockholm (AWS eu-north-1, `16.170.208.231`) has a real, physically-verified AWG+REALITY+TLS_TCP data plane. **B15: Stockholm's `pocvpn-api` control-plane is now deployed and live** - a real `pocvpn-api` instance runs on the Stockholm VPS itself (systemd-managed, `127.0.0.1:8443` behind its own nginx TLS edge on `16.170.208.231`, configured exclusively from Stockholm's own facts per `gateway/DEPLOYMENT.md`'s "second gateway" section, zero code changes), and `POST /v1/activate`/`POST /v1/xray-profile` were verified live: real AWG peer creation (fail-closed auth confirmed: no/wrong bearer -> 401, GET -> 403, unknown route -> 404), real REALITY identity issuance, and real TLS/TCP identity issuance, all through the actual control-plane HTTP path (synthetic device credentials, not the release Android app - no physical device was available to drive this deployment's own verification pass; a real on-device activation is still the outstanding step before Stockholm's AWG is enabled in `GatewayPickerDialog` for any real device). Two real deployment bugs were found and fixed only by actually running this live (neither was exercised by the B8B1C3 local/WSL harness): `POCVPN_API_PROVISION_SCRIPT_PATH` must point at the sudoers-permitted wrapper (`/usr/local/libexec/pocvpn-provision-peer`), never at `provision-peer.sh` directly; and `pocvpn-api.service`'s `ProtectSystem=strict` sandbox needed `/etc/nova-xray` added to `ReadWritePaths` (alongside the existing `/etc/amnezia/amneziawg` exception) for the same reason - the root child `sudo -n nova-xray-reload` spawns to publish the live Xray config inherits this unit's mount namespace and cannot write outside its `ReadWritePaths`, even as root. The REALITY private key was rotated during this deployment (the prior operator/debug-provisioned key was replaced after being transiently exposed in an operator's own tooling output while deriving its public counterpart - not a data-plane compromise, and Germany's REALITY key is untouched); Stockholm's AWG server keypair and REALITY short-id/dest/serverName were reused unchanged. (C) **B15 (2026-08-31, completed)**: Stockholm's AWG is now REAL and SELF-SERVICE on the physical device it was tested on. Two things had to be found and fixed to get here, neither of them the control-plane code itself (already gateway-agnostic since B14): first, `GatewayPickerDialog` had no way to REQUEST activation for a gateway once ANY gateway was already provisioned (`screenFor()`'s ACTIVATION/HOME choice only fires for a device with zero provisioned gateways at all) - fixed with the smallest correct change, an "Activate" action on an unprovisioned row that opens the existing `ActivationScreen` (now with an optional `onCancel`) targeted at that specific gateway id. Second, Stockholm's `pocvpn-api` HTTPS edge (port 443) was deployed correctly on the VPS but not reachable from the internet at all - an AWS security-group gap (80/2053/2083/51820 had inbound rules from earlier slices, 443 did not), fixed by the operator directly in the AWS console (outside this codebase, no infrastructure-as-code exists for it yet). With both fixed, a real activation on the physical test device succeeded end to end: `POST /v1/activate` and `POST /v1/xray-profile` (both transports) all returned 200 from the real Android client (confirmed in the server's own access log by User-Agent and real device public IP, not synthetic curl), `GatewayPickerDialog` now shows Stockholm as a normal selectable row (no longer "Unavailable on this device"), AWG connected with a real handshake (`awg show` on the server confirmed the real peer/endpoint/live data transfer), REALITY and TLS were each independently exercised via the debug-only `XrayDiagnosticsActivity` reading the REAL provisioned profile (not a fabricated one) - the server's own Xray log shows real proxied traffic tagged with this exact activation's id for both `nova-vless-reality-in` and `nova-vless-tls-in` - and Stockholm's provisioned/selected state survived a full app process restart (force-stop + relaunch). Germany was reconnected immediately after and worked unaffected. (D) Stockholm's REALITY/TLS profiles previously on-disk from operator/debug-provisioning (B13) were superseded by B15's control-plane deployment - the live Xray config is now rendered/owned entirely by `pocvpn-api`'s own activation pipeline (revocation-aware, per-device identity), not the old manual single-client config. (E) **B16 (2026-09-01) update - superseded**: automatic multi-gateway selection/failover now EXISTS as real, pure decision logic, live-wired for real connect() attempts behind a device-local `gatewayAutoMode` toggle (default off/Manual), and has now been PHYSICALLY VALIDATED on a real device (real Auto connect to Germany, a real on-device fault making Germany unprovisioned, real Auto failover to Stockholm with a confirmed real `16.170.208.231` data-plane connection, restore, confirmed normal reconnect) - see the dedicated `automatic gateway selection / failover` row below for the full detail and its own honest caveat about which specific failure-trigger path remains unit-test-only. Manual-only selection (the pre-B16 default and always-available fallback) is completely unaffected. (F) Gateway Pool stays FOUNDATION, not IMPLEMENTED, solely because `16.170.208.231` is Stockholm's auto-assigned public IPv4 (not a purchased/reserved Elastic IP) - a stop/start could reassign a different address and invalidate this catalog entry and its IP-bound TLS certificate, so this data plane is validation/foundation infrastructure, not yet durable production addressing (no AWS resource was purchased/changed to fix that in this pass - a deliberate scope boundary, not an oversight). --- HISTORY below records how this state was reached; where it conflicts with CURRENT STATE above, CURRENT STATE is authoritative. --- B12 - the typed multi-endpoint model is real and proven end-to-end: `EndpointManifest` already accepted multiple `EndpointDescriptor`s since B11, and B12's `GatewayPoolIntegrationTest` proves a manifest naming two DISTINCT endpoint IDs/providers/ASNs flows correctly through canonicalization, `ReachabilityEngine`, `PathCandidateBuilder`, and `PathScorer` together (not just each stage in isolation). At the time (B12), kept at FOUNDATION, not IMPLEMENTED, because exactly ONE real production endpoint existed (`frankfurt`) - a second real gateway needed a real second VPS from a different provider/ASN, an explicit operator action out of scope for an automated change (see `docs/B12_ENDPOINT_IDENTITY_AUDIT.md` for what that would additionally require on the credential/activation side); no fabricated second endpoint was embedded in production data. **This specific claim is now HISTORICAL, not current** - B13 below deployed that real second VPS (Stockholm); see CURRENT STATE at the top of this row for what's true today. **B13 - the credential/activation-side design question that audit raised is now partly answered in code**: `XrayProfileRepository`/`XrayTlsProfileRepository`'s backing files are now endpoint-scoped (`FileXrayProfileStore`/`FileXrayTlsProfileStore` key by `EndpointId`, option (a) from that audit - independent per-gateway credentials, never a shared/federated identity), with a one-time, safe migration of the single pre-B13 unscoped file into the production endpoint's own scoped slot (`XrayProfileRepositoryFactory.create(..., migrateFromLegacyUnscopedFile = true)`, the composition root's only call site for it) and isolation/migration regression tests (`XrayProfileRepositoryTest`/`XrayTlsProfileRepositoryTest`) proving two endpoints' credentials in the same directory never collide. At the time, still only ONE repository instance was actually constructed/wired (there was nothing to select between yet) - this was storage-layer readiness for a second gateway, not a second gateway. **This specific claim is now HISTORICAL, not current** - a Stockholm-scoped repository pair is wired today (see CURRENT STATE (D) at the top of this row, and the Xray/TLS endpoint-availability fix below). **B13 (2026-08-30 correctness audit)** - the RUNTIME layer now genuinely threads endpoint identity too, closing a gap the audit found where the storage layer above was endpoint-scoped but the actual Xray execution path (`VlessRealityTransport`/`VlessTlsTransport`/`NovaXrayVpnService`) still silently read one repository fixed at construction time: `VpnController.buildTransportConfig` now resolves via a new `XrayProfileRepositoryResolver`/`XrayTlsProfileRepositoryResolver` keyed by the CURRENT attempt's real endpointId (never `ProductionGateway.ID` hardcoded at the selection site), that endpointId is threaded into `TransportConfig.Xray/XrayTls`, `VlessRealityTransport`/`VlessTlsTransport` resolve their own pre-flight repository from it and pass it to `NovaXrayVpnService` via a new `EXTRA_ENDPOINT_ID` intent extra, and `NovaXrayVpnService` rebuilds its `XrayCoreController` only when the requested endpoint actually changes (`VpnControllerXrayEndpointResolverTest`, `NovaXrayVpnServiceEndpointParsingTest`). An endpoint with no configured repository fails closed (`XrayProfileNotReadyException`), never silently substituting a different endpoint's credential. Zero behavior change for today's single production endpoint - every new default derives from it. **B13 (2026-08-30, second real gateway)** - a real, independently-provisioned second AWG gateway now exists (AWS, Stockholm, `16.170.208.231`, different provider/ASN from the Oracle/Frankfurt gateway). **At the time this was written it was described as "a genuine, user-selectable product option" - that is now HISTORICAL, not current: see CURRENT STATE (C) at the top of this row for why Stockholm's AWG is actually disabled in the release UI today.** The infrastructure this paragraph documents is real: `ProductionGatewayCatalog` (Germany/Stockholm, each with its OWN AWG connection facts and its OWN AWG obfuscation profile - see that class's own docs for why a single global `PocAwgProfile` was a real bug, not a simplification), `SelectedGatewayStore`/`SelectedProductionGatewaySource` (persisted, deterministic manual selection - a real UI picker replaces the old static "Germany / Frankfurt" placeholder), and `SmartConnectCandidateSelector.productionGatewayCandidates` now attributes `ConnectionOutcome`/`PathHistory` to whichever gateway is ACTUALLY selected, never a hardcoded default. AWG on Stockholm is physically handshake- AND data-plane-verified (real device, real 8-second timeout window, 0% ping loss to the gateway's own tunnel address, exit IP `16.170.208.231` confirmed via icanhazip.com/Cloudflare trace, DNS resolves through the tunnel with no leak, IPv6-only fetch fails closed). AWG on Germany was found, via read-only SSH diagnosis of the live gateway, to have a stale client-side `clientTunnelIp` (`10.77.0.2`, a value that had since been reassigned server-side to a DIFFERENT peer by `add-peer.sh`) - the real, current server-side peer registration for this device's public key is `10.77.0.5/32`. This is a pure client-config mismatch (WireGuard's AllowedIPs is both a route table and a source-IP ingress filter, so the handshake succeeded while every data packet was silently dropped) - gateway A's firewall/NAT/forwarding/ip_forward were all already correct, and NO server-side change was made. Fixed at the time by editing the (then-hardcoded) `ProductionGatewayCatalog.GERMANY.awg.clientTunnelIp`; a later review correctly found that field itself was the wrong home for it (a per-device provisioned peer address, not a gateway fact) and it now lives in the dedicated, per-endpoint `ClientTunnelIdentityStore` instead (see that class's own docs) - Germany is now physically re-verified with the SAME full evidence bar as Stockholm (0% ping loss, exit IP `152.70.43.1`, DNS/IPv6 checks), and Stockholm re-verified immediately after to prove the fix didn't regress it. Provider/ASN names (Oracle Cloud/AWS) are internal-only fields now - the normal gateway picker/LocationCard/Home screen show geography only ("Germany / Frankfurt", "Sweden / Stockholm"); provider metadata is still available in the debug diagnostics dialog. **B13 (2026-08-30, Stockholm REALITY/TLS)** - a real Xray REALITY inbound (port 2053, camouflage dest `www.wikipedia.org:443`, matching the SAME discipline gateway A's own REALITY inbound uses) and a real TLS/TCP inbound (port 2083, a genuine Let's Encrypt short-lived IP certificate for `16.170.208.231` - the newly-GA ACME `ip-address`/`shortlived` profile, auto-renewing via a `certbot renew` deploy hook mirroring gateway A's own `pocvpn-nova-xray-cert.sh`) are now live on Stockholm, with a FRESH REALITY keypair and VLESS UUID generated server-side (never derived from or shared with Germany's credentials) and stored in the SAME endpoint-scoped `XrayProfileRepository`/`XrayTlsProfileRepository` storage this codebase already built for exactly this purpose (`EndpointId("stockholm")`, isolated from `EndpointId("frankfurt")`). Both physically verified on the real device: real Xray core start, real `VPN CONNECTED`/`EVER_VALIDATED` network state, exit IP `16.170.208.231` for both transports, DNS resolving through the tunnel (no leak), IPv6-only fetch failing closed. Germany re-verified working immediately afterward (AWG unaffected). **Deliberately NOT deployed for Stockholm**: the `pocvpn-api` control-plane (real HTTP activation/`/v1/activate`/`/v1/xray-profile`) - this repo's own `gateway/DEPLOYMENT.md` documents no production installer exists for it even on gateway A's own history, and building one was out of this slice's scope. Credentials were provisioned directly server-side and saved into the SAME on-device encrypted stores a real activation would populate, via a debug-only manual-save path added to `XrayDiagnosticsActivity` (now endpoint/transport-aware, debug-build-only, absent from release). This means Stockholm's REALITY/TLS are real, physically-verified DATA-PLANE transports, but NOT yet reachable via the app's real self-service activation flow - a real second-gateway control-plane deployment remains a distinct, larger future slice. Kept at FOUNDATION, not IMPLEMENTED, for exactly that reason. **B13 (2026-08-31, consolidated correctness pass)** - a final review found the per-device `ClientTunnelIdentityStore` fix above had itself regressed to inventing/hardcoding this device's own IPs for every install; that migration is now genuinely evidence-based (`MainViewModel.activateDevice` maps a live activation response to a `ProductionGatewayId` from its FULL stable server facts - host+port+key together, `ProductionGatewayCatalog.matchGatewayId` - never endpointHost alone and never the UI's current selection; a response matching no known gateway is REJECTED, never silently accepted-but-ignored) and `GatewayPickerDialog` now visibly disables any gateway this device has no client tunnel identity for ("Unavailable on this device"), backstopped by `MainViewModel.selectGateway`/`isGatewayProvisioned` and a startup+post-activation reconciliation (`reconcileSelectedGateway`/`reconcileSelectedGatewayIfNeeded`) that switches a now-unusable persisted selection to a provisioned gateway with no app restart required, or leaves it alone (never inventing an identity) if nothing is provisioned yet. **Truthful current-state consequence**: because no genuine pre-B13 local evidence can exist for Stockholm's AWG client tunnel IP (it is not the endpoint the legacy single-gateway activation flow ever provisioned), Stockholm's AWG remains UNPROVISIONED - and therefore disabled in the picker - on EVERY device, including the one this feature was physically validated on, until it goes through a real provisioning path (a genuine second-gateway control-plane activation, or an explicit debug/operator-only write) neither of which exists yet; no debug helper for this was added in this pass, so that re-provisioning is a deliberately deferred follow-up, not a regression to fix silently. This is separate from Stockholm's REALITY/TLS profiles above, which remain on-disk from their own physical validation and are now correctly, independently exposed per-endpoint (see the Xray/TLS availability fix below) - only Stockholm's *AWG* client tunnel identity is affected. Also fixed in this pass: `MainViewModel.buildTransportRegistry`'s XRAY_REALITY/TLS_TCP availability was a single global flag tied only to Germany's own repository, so Germany having a profile could make Stockholm's registry falsely report AVAILABLE (and vice versa) even though the AWG->Xray failover path would then fail to actually resolve a transport for the wrong endpoint; availability is now a `Set<EndpointId>` checked per the CURRENT attempt's real target (`isXrayAvailableFor`/`isXrayTlsAvailableFor`), with a Stockholm-scoped repository instance now wired in the Factory (reading the SAME on-disk file the debug-only `XrayDiagnosticsActivity` manual-save path already writes to) so Stockholm's real, already-provisioned REALITY/TLS profiles are correctly seen as available for Stockholm specifically. Also fixed: the picker/Home no longer permits changing `selectedGateway` while a session is CONNECTING/CONNECTED/RECONNECTING/DISCONNECTING (`TransportState.blocksGatewaySelection`) - previously the picker could silently change the DISPLAYED location while the active tunnel kept exiting the previous gateway. **B14 (2026-08-31, Stockholm self-service provisioning - code path)** - the real self-service Stockholm control-plane request path now exists end to end on the client: `MainViewModel.activateDevice(activationCredential, targetGatewayId)` takes an EXPLICIT target (defaulted only to the currently selected gateway for every pre-B14 1-arg call site), `ProvisioningClient` gained endpoint-aware 3-arg overloads of `activate`/`fetchXrayProfile`/`fetchXrayTlsProfile` that POST to the REQUESTED gateway's own edge host (sourced from `ProductionGatewayCatalog`, never a second/separately-maintained literal), and a successful response is required to unambiguously match the REQUESTED gateway specifically via `matchGatewayId` (now including `gatewayTunnelIp` in the match, closing a gap where a response with the right host/port/key but a wrong/stale `gatewayTunnelIp` could have been silently accepted) - a response that matches nothing, or that validly matches a DIFFERENT known gateway than the one requested, is rejected outright, exactly like an unmatched one. A successful, matched Stockholm activation writes `ClientTunnelIdentityStore[STOCKHOLM]` only (never touches Germany's entry) and runs Stockholm's OWN `XrayProfileProvisioner`/`XrayTlsProfileProvisioner` pair against Stockholm's OWN endpoint-scoped repositories (never Germany's, never `XrayDiagnosticsActivity` - that stays debug-build-only, confirmed absent from the release manifest) - `isGatewayProvisioned(STOCKHOLM)` then genuinely becomes true and `GatewayPickerDialog` enables it, with the SAME startup/post-activation reconciliation, restart-durability (`FileClientTunnelIdentityStore`), and active-session-selection-blocking guarantees Germany already had. Audited the gateway control-plane codebase (`gateway/api/config.py`/`handler.py`) and found it already fully env-var-driven per instance with zero Germany-specific hardcoding - so **no server-side code changes were needed or made**; supporting a second gateway is purely a deployment action (see `gateway/DEPLOYMENT.md`'s own new "second gateway" section). **No `pocvpn-api` instance was deployed to Stockholm in this slice, and no AWS resources were allocated, purchased, or changed** - a real Stockholm activation attempt today genuinely fails closed with a network error, exactly as it should until an operator performs that deployment. This is "self-service provisioning implemented in code", explicitly NOT "self-service provisioning live" - see the `multi-provider gateway infrastructure` row's own (2)/(3) split. No physical device retest was performed or required: this slice adds a new, currently-unreachable request path, it does not alter Germany's already-validated data-plane/runtime behavior, and Stockholm's own already-physically-validated data plane (AWG/REALITY/TLS_TCP) is untouched. |
| Gateway Health / Reachability | **IMPLEMENTED for Automatic gateway selection** (Manual-gateway-mode's own transport picker remains static - see Smart Connect row) | `TransportHealth` (Phase 2A) is a real typed model (`UNKNOWN`/`HEALTHY`/`DEGRADED`/`UNREACHABLE`/`NOT_IMPLEMENTED`), now populated with real, transport-level evidence: `TransportHealthCalculator` (B8L1) derives it from `ConnectionOutcomeStore`'s real per-attempt history. `SmartConnectDecisionEngine.decideAuto` (Manual-gateway-mode's own transport picker) still doesn't act on `health` (see Smart Connect row above). **B19 (2026-09-01) update: for AUTOMATIC gateway selection specifically, health/reachability ARE genuinely decision-driving** - `AutoGatewaySelector` (reusing `PathScorer`/`ReachabilityEngine` verbatim, never a parallel scorer - see that row's own docs) has ranked candidates by real `TransportHealth`/`EndpointReachability`/local history/capability maturity since B16; B19 adds a real, unit-proven demonstration (`AutoGatewaySelectorTest`'s "fresh AWG UNREACHABLE plus REALITY REACHABLE ranks REALITY first" / "AWG DEGRADED plus REALITY HEALTHY reorders past static preference" / "all UNKNOWN falls back to static preference" cases) that this reordering genuinely happens end to end, not merely at the `PathScorer` unit level. B11 adds the genuinely PER-ENDPOINT counterpart this row used to lack - see Endpoint / Path Reachability Fabric below, kept as its own row rather than merged into this one because `EndpointReachability` answers a materially different question than `TransportHealth` (this endpoint, this transport, this network - vs this transport in general). B12 further splits per-endpoint evidence itself: `ReachabilityEvidenceSummary.controlPlaneReachable` (the gateway's HTTPS API - manifest/activation/provisioning) is now a field DISTINCT from `endpointSpecificReachable` (a real data-plane attempt outcome for that exact endpoint+transport, now derived from `ConnectionOutcomeStore`'s own per-attempt `gatewayId`/`transport` fields rather than reusing the control-plane probe for both) - the task's own "do not collapse control-plane/endpoint/transport signals" requirement, unchanged and still honored by B19's own additions (neither the new diversity fix nor the new cooldown penalty ever reads or writes `controlPlaneReachable`). **B13** - this evidence is now actually surfaced in the debug diagnostics UI (`AppRoot.reachabilityDiagnosticsLines`, `DiagnosticsDialog`): manifest version/source/expiry, per-endpoint id/roles/region/provider/ASN/transports, per-(endpoint,transport) reachability state with control-plane and endpoint-specific evidence and their independent ages, and ranked `PathCandidate`s with type/eligibility/score/reasons - built generically by iterating `ReachabilityDiagnosticsSnapshot`'s own lists, so it already renders N endpoints correctly even though only one real one exists today. **B19** - the live Auto-mode diagnostics line (`AppRoot`'s `autoCandidatesLine`) now also surfaces each ranked candidate's typed reason tokens (`PathScorer.Reason` - `ENDPOINT_REACHABLE`/`TRANSPORT_HEALTHY`/`FAILURE_COOLDOWN`/`DIVERSITY_BONUS`/etc, task requirement 14's own vocabulary), not just gateway/transport/score. No secret/credential field is ever included (the underlying types structurally carry none). **Physically validated end to end (2026-09-01) - promoted to IMPLEMENTED for Automatic gateway selection.** On the real physical test device, in real Auto mode with transport preference genuinely `Auto` (never pinned): (A) baseline - the live Diagnostics `Auto candidates (ranked)` line showed `GERMANY/AMNEZIA_WG(score=2029973,ENDPOINT_UNKNOWN,TRANSPORT_HEALTHY)` ranked first; connecting normally produced `Current transport: AMNEZIA_WG`/`Gateway: Germany / Frankfurt` with a real 623ms handshake - the executed attempt matched candidate #1 exactly. (B) controlled dynamic reordering - two REAL `FAILURE` outcomes for Frankfurt AWG were written into `ConnectionOutcomeStore`/`FilePathHistoryStore` via a genuinely minimal debug-only mechanism (`MainViewModel.debugRecordConnectionFailure`, one more button in the existing `isDebugBuild`-gated Diagnostics dialog - writes through the EXACT SAME stores/write path a real failed connection attempt already uses, never a fake/parallel evidence channel; no server config, no AWG peer config, no provisioning identity touched). The NEXT freshly-built Auto plan showed `GERMANY/AMNEZIA_WG(score=-1080,ENDPOINT_UNREACHABLE,TRANSPORT_UNREACHABLE,RECENT_FAILURE_THIS_NETWORK,FAILURE_COOLDOWN)` demoted to LAST place, with `GERMANY/XRAY_REALITY(score=2020000,...)` now ranked FIRST; connecting produced `Current transport: XRAY_REALITY`/`Gateway: Germany / Frankfurt`, `Protected` - the executed attempt again matched the new candidate #1 exactly, with no `AwgXrayFailoverPolicy`/manual-picker involvement (transport preference stayed `Auto` throughout - the reordering came entirely from ranking evidence, not a fallback policy). (C) restore - a real `debugRecordConnectionSuccess` write (mirroring what a real successful AWG connection already produces) restored the plan to `GERMANY/AMNEZIA_WG(score=3030975,ENDPOINT_REACHABLE,TRANSPORT_HEALTHY,RECENT_SUCCESS_THIS_NETWORK)` ranked first again; a real (non-simulated) connect through the actual UI produced a genuine 573ms AWG handshake, `Current transport: AMNEZIA_WG`, `Protected` - proving AWG returns ahead of REALITY once dynamic evidence supports it, executed via the real UI path, not the debug helper. (D) cooldown evidence - the SAME fault from (B) already showed `FAILURE_COOLDOWN`/`RECENT_FAILURE_THIS_NETWORK` typed reason tokens on the demoted candidate, and the SAME restore from (C) showed them replaced by `RECENT_SUCCESS_THIS_NETWORK` with the score jumping from -1080 to 3030975 - physical proof of both the bounded cooldown penalty and its clearing-on-success, with no additional invasive faulting needed. Device left clean: Auto gateway mode on, transport preference `Auto`, no debug fault active, `Protected` via a real AWG connection. Manual-gateway-mode's own transport picker remains untouched/static (see Smart Connect row) - this promotion applies to Automatic gateway selection specifically, never claimed for Manual mode. **B19-3 (2026-09-01) eligibility clarification**: a candidate with FRESH endpoint-specific `ReachabilityState.UNREACHABLE`, or `TransportHealthState.NOT_IMPLEMENTED`, is now genuinely INELIGIBLE (`PathScoreResult.eligible = false`) rather than merely low-scored-but-still-executable - `AutoGatewaySelector` already only ever promotes `eligible == true` results into the executable plan, so this closes a real gap where a confirmed-dead path could still occupy a `MAX_ATTEMPTS` slot. Transport-wide `UNREACHABLE` is ineligible UNLESS a fresh, stronger, endpoint-specific `REACHABLE` read overrides it - the one explicit precedence rule `PathScorer.isEligible`/`ineligibilityReason` enforces, never duplicated elsewhere. `DEGRADED`/`UNKNOWN` remain eligible, and stale evidence still decays to `UNKNOWN` (hence eligible again) entirely within `ReachabilityEngine` - `PathScorer` has no second freshness check. `PathScorerTest`'s own eligibility cases (fresh UNREACHABLE ineligible regardless of health; endpoint REACHABLE overriding transport-wide UNREACHABLE; endpoint UNKNOWN with transport-wide UNREACHABLE staying ineligible; DEGRADED/UNKNOWN eligible; NOT_IMPLEMENTED ineligible; stale-decayed UNREACHABLE eligible via a real `ReachabilityEngine.assess` call) and `AutoGatewaySelectorTest`'s own proof (a fresh-UNREACHABLE AWG candidate is entirely ABSENT from the plan and `nextCandidate` never returns it) cover this directly. Full suite (886 tests) passes; not re-validated physically this pass (no ranking-order/execution-path behavior changed for any ALREADY-eligible candidate - only genuinely-dead candidates are now excluded outright instead of merely last-ranked, and the existing physical pass already proved that ranking order drives real execution). |
| Endpoint / Path Reachability Fabric | **FOUNDATION** | B11 - the first real endpoint/path model. `EndpointDescriptor`/`EndpointRole`/`EndpointManifest` (typed, provider/ASN-neutral, no hardcoded commercial infrastructure) are signed (Ed25519) and verified (`Ed25519ManifestVerifier`: signature, expiry, clock-skew, monotonic-version rollback protection - `ManifestRollbackGuard`) via `EndpointManifestRepository`, which is backed by a durable last-known-good store (`FileLastKnownGoodManifestStore`) that falls back to a REAL, cryptographically signed embedded bootstrap manifest (`EmbeddedBootstrapManifest`, signed offline by `gateway/tools/manifest_signing.py` - never an unsigned fallback) whenever no valid LKG exists yet - `EndpointManifestRepository.trustedState()` fails closed (`TrustedManifestState.NoneTrusted`) if NEITHER verifies, never silently trusting an unverified manifest merely because it's embedded in the APK. `ReachabilityEngine` derives per-(endpoint, transport) `EndpointReachability` from evidence this codebase already collects - deliberately distinct from `TransportHealth` (see that row's own note) and deliberately conservative (stale evidence and no-network both fall back to UNKNOWN, never a stronger claim). `PathCandidateBuilder` builds `Direct` (today's real shape - see Gateway Pool below for the B12 multi-endpoint proof) and `Relayed` (Client->INGRESS->EXIT, modeled but NOT a working relay protocol) candidates from the manifest's own role/`relayTo` relationships. `PathScorer` ranks them with reachability as the dominant, order-of-magnitude-separated factor (transport health, this-network local history, capability maturity, then small latency/failure/diversity adjustments - each tier's dominance analytically re-derived, not merely asserted, after a real gap was found and fixed in review - see that object's own docs). Network-specific local connection memory (`FilePathHistoryStore`, keyed `networkFingerprint x endpointId x transportKind`) uses an app-local, per-install AndroidKeyStore-HMAC network fingerprint (`NetworkFingerprinter`, deduped/order-independent over coarse signals) - never SSID/BSSID/IMSI/DNS-query history, and not a globally reusable id. **B13 re-audited this and found no violation** - `NetworkProfiler` only ever reads `ConnectivityManager`/`LinkProperties` (network type, resolver IPs, no `WifiInfo`/`TelephonyManager`), `CoarseNetworkSignals` itself never reaches disk (only its derived fingerprint string does, via `PathHistoryStore`), and a new regression test (`NetworkFingerprinterTest`) proves the on-disk `path_history.bin` bytes contain the derived fingerprint but never the raw resolver address that produced it. B12 adds a REAL manifest distribution channel: `GET /v1/manifest` (`gateway/api/handler.py`) serves an operator-deployed, already-signed artifact byte-for-byte - this gateway process never signs or holds a private key - and Android's `HttpsRemoteManifestFetcher`/`ManifestDistributionClient` fetch+`offer()` it through the SAME verification/rollback boundary any other candidate uses (see `docs/B12_MANIFEST_KEY_CEREMONY.md`). Both ends are unconfigured by default (`POCVPN_API_MANIFEST_PATH`/`BuildConfig.MANIFEST_URL` blank) - no production key ceremony has been performed, so nothing live actually serves a real manifest yet. Endpoint-specific data-plane evidence (`ReachabilityEvidenceSummary.endpointSpecificReachable`) now carries its OWN freshness (`endpointSpecificReachableAgeMillis`, independent of `transportHealthAgeMillis`) and expires against an explicit TTL (`ReachabilityEngine.assess`'s `endpointEvidenceStaleAfterMillis`) - an old outcome falls back to the transport-wide health mapping rather than continuing to override current reachability forever (fixed during PR #24 review; see `ReachabilityEngineTest`'s staleness cases). **`PathScorer`'s provider/ASN diversity bonus was real and tested at the `PathScorer` level (`PathScorerTest`) but DISABLED at its one live call site through B18** (`MainViewModel.reachabilityDiagnostics()` still unconditionally passes `diverseProviderOrAsnSeenElsewhere = false` for its own OBSERVATIONAL diagnostics computation - the batch-wide Boolean PR #24's review found gave every candidate an identical bonus, which had no effect on ranking, was never fixed there). **B19 (2026-09-01) fixes the REAL decision-driving call site**: `AutoGatewaySelector.buildCandidates` now computes a genuine PER-CANDIDATE diversity signal - a small internal pre-pass identifies which manifest-declared provider/ASN keys already have fresh negative evidence in this SAME batch (degraded/unreachable transport health or endpoint reachability, or an active this-network failure streak - see Connection Memory row's own B19 update), and only a candidate on a DIFFERENT, non-troubled provider/ASN receives the bonus, and only when a genuinely troubled alternative exists at all (`AutoGatewaySelectorTest`'s "clean provider gets the bonus only when a troubled provider exists"/"no bonus when nothing is troubled"/"a troubled provider never grants itself the bonus" cases). `MainViewModel.reachabilityDiagnostics()`'s own OBSERVATIONAL-only call site is unchanged (still `false`, still harmless, since nothing acts on its output) - only the real decision-driving Auto path was worth fixing correctly. **Live-wired, OBSERVATIONAL ONLY**: `MainViewModel.reachabilityDiagnostics()` computes real, fresh data on every call. `refreshManifest()` is now ACTUALLY invoked in production - fixed during PR #24's second review, which found it was constructed (`ManifestDistributionClient`) but never triggered, so a configured `MANIFEST_URL` alone did nothing. `MainViewModel`'s `init{}` block now fires it exactly once per ViewModel instance (the same one-time-startup pattern already used for `xrayProfileRepository`'s own init check - never a timer, never re-triggered by Compose recomposition), guarded by a `Mutex` so a concurrent/repeated signal is skipped rather than causing a fetch storm; a failure leaves LKG/bootstrap untouched (`EndpointManifestRepository.offer()`'s own unchanged contract) and nothing here gates or delays `connect()`. Both remain unconfigured by default (`POCVPN_API_MANIFEST_PATH`/`BuildConfig.MANIFEST_URL` blank), so in practice nothing fetches yet - but the trigger itself is real, not merely constructed-and-idle. Nothing in `SmartConnectDecisionEngine`/`AwgXrayFailoverPolicy`/automatic transport selection reads either accessor - kept at FOUNDATION rather than IMPLEMENTED specifically because promoting a winning `PathCandidate` into the real decision boundary is left to a deliberate future promotion slice. `reachabilityDiagnostics()` now builds `Relayed` candidates too whenever the trusted manifest actually expresses an INGRESS->EXIT `relayTo` relationship (fixed alongside an unsafe `as PathCandidate.Direct` cast found in PR #24's second review - the scoring loop is now an exhaustive `when` over both variants), proven with a real dedicated regression test (`MainViewModelTest`). Only `Direct` candidates over the single real pinned gateway exist in PRODUCTION DATA today, since no real manifest declares a relay relationship yet - `Relayed` is exercised by tests, not real traffic. **B23 (2026-09-02, PR #37 review fix applied 2026-09-02) - Restricted-Network / Whitelist Bridge Runtime Foundation, still FOUNDATION.** Turns `Relayed` from an observational-only shape into a real, evidence-scored EXECUTABLE CANDIDATE MODEL - deliberately NOT described as "decision-integrated": see the explicit "not yet consumed" note below. `IngressKind` (`DIRECT_IP`/`CDN_FRONTED`, never a named third-party provider) is now typed and stored through `EndpointTransportBinding.metadata`'s existing extensibility - not a new manifest binary field, so every already-signed manifest (including the embedded production bootstrap) keeps verifying byte-for-byte with zero re-signing ceremony. **PR #37 review fix**: the ingress (client-facing) and exit/upstream transports are now pinned INDEPENDENTLY - `PathCandidate.Relayed.transport` (client<->ingress) and the new `PathCandidate.Relayed.exitTransport` (ingress<->exit) are never assumed equal, `PathHop` now carries its own exact, immutable `EndpointTransportBinding` per hop, and `PathCandidateBuilder.buildRelayed` takes separate `ingressTransport`/`exitTransport` parameters (an earlier version of this row wrongly collapsed both hops onto one shared `TransportKind` - fixed before merge, never shipped to `main`). `PathCandidate.historyPathId` closes the exact gap this row's own prior note flagged as deliberately deferred: `Relayed` now gets a real composite key encoding BOTH hop endpoints AND both hop transports (`"ingressId:ingressTransport->exitId:exitTransport"` - never conflated with either hop's own `Direct` history, never shared between two relays through the same ingress to different exits, and never collided across two paths sharing endpoints but differing in either hop's transport), and `reachabilityDiagnostics()`'s Relayed candidates get real local history credit instead of a hardcoded `null`. `AutoGatewaySelector.buildRelayedCandidates` (new, alongside the unchanged `buildCandidates`) is a real, callable relay-scoring function on that object, reusing `PathCandidateBuilder.buildRelayed`/`ReachabilityEngine`/`PathScorer` verbatim - never a parallel engine - scoring every (ingress transport, exit transport) PAIR the manifest supports independently, and requiring no `ProductionGatewayCatalog`/AWG provisioning entry (none exists for a real RU ingress, since none is deployed this slice). **PR #37 review fix, round 2**: `RelayAttemptCandidate` now also carries `ingressBinding`/`exitBinding` - the EXACT, immutable `EndpointTransportBinding` each hop was pinned against at build time, copied straight off `PathCandidate.Relayed.ingress.binding`/`.exit.binding`, never re-looked-up by endpointId/TransportKind - the same B16 attempt-pinning invariant `GatewayAttemptCandidate.configSnapshot` already enforces for Direct, closing the gap where a future execution layer would otherwise have had to re-resolve host/port facts from a manifest that could have rotated since this candidate was built. Real evidence alone produces the required outcomes with no invented "prefer relay under restriction" rule: `PathScorer`'s existing reachability-tier dominance means UNKNOWN ingress evidence never outranks a healthy Direct candidate, a fresh ingress/path failure excludes the relay outright (never merely low-scores it), stale failures decay back to UNKNOWN via the same `ReachabilityEngine` freshness gate every other endpoint already uses, and a hard-whitelist network's own evidence naturally makes Direct ineligible while a proven-reachable ingress stays eligible - proven by a real, focused test suite (`PathCandidateBuilderTest`/`PathHistoryStoreTest`/`AutoGatewaySelectorTest`'s new B23 cases, including dedicated independent-transport-pair coverage). **Explicitly NOT done this slice, and NOT consumed by the live connect path**: `MainViewModel.connectAuto()`/`attemptAutoCandidate()` and `VpnController` do not call `buildRelayedCandidates` and cannot execute a `RelayAttemptCandidate` - `buildRelayedCandidates` is callable/tested in isolation only, never reached from any real connection attempt today. Wiring execution now would be dead code no physical test could exercise (no real RU ingress exists to dial); final promotion into the real Auto selection/execution decision remains deferred to the slice that deploys a real ingress runtime. **Real restrictive-network/Russia whitelist bypass remains UNVERIFIED** - nothing in this slice claims otherwise. |
| Real Ingress Runtime / Server-Side Relay Execution | **FOUNDATION** (B25, 2026-09-02 update) | **CURRENT STATE (B25, 2026-09-02) - closes every remaining CLIENT/control-plane blocker B24 named, still FOUNDATION: no real RU ingress is deployed, so none of this is physically exercised, and Russia hard-whitelist bypass remains UNVERIFIED.** (A) **Typed session identity (task A)**: `net.pocvpn.client.relay.VpnAttemptContext` (`Direct`/`Relayed(plan)`) is pinned exactly once per attempt into `TransportOrchestrator.Resolution.Resolved.attemptContext` (default `Direct`, every pre-B25 caller unaffected) and carried into `VpnController.pendingAttemptContext` (reset on `connect()`/`disconnect()`, never guessed from transport kind/endpoint name). (B) **Real Protected gating (task B)**: `net.pocvpn.client.vpn.VpnSessionHealth` (`DirectProtected`/`RelayHandshake(stage)`/`RelayProtected`/etc) is computed by the pure `computeSessionHealth(state, attemptContext, relayStage)` and exposed as `VpnController.sessionHealth`/`MainViewModel.sessionHealth`; `ProductFlowPresentation.toHomeStatusText()`/`toHomeVisualState()` now have `VpnSessionHealth` overloads `AppRoot`'s Home screen reads instead of the raw `TransportState` ones - for Direct, `DirectProtected` fires under the EXACT same condition `TransportState.Connected` always did (byte-for-byte unaffected); for Relayed, `TransportState.Connected` alone (ingress-handshake only) reports `RelayHandshake`, NEVER `RelayProtected`, until a real `RelayReadinessStage.END_TO_END_DATA_PLANE_OK` is reported via `VpnController.reportRelayStage`. (C) **Real end-to-end proof contract (task C)**: `RelayEndToEndProbe` (a real `HttpRelayEndToEndProbe` implementation issuing an authenticated HTTPS GET to `IngressClientProfile.endToEndProbeUrl`, over whatever route the OS resolves - genuinely through the just-established tunnel, never a bespoke socket) plus a fail-closed `NotConfiguredRelayEndToEndProbe` default (`EXECUTION_NOT_IMPLEMENTED` for every attempt - the production default until a real ingress issues real probe coordinates). `MainViewModel.armFailoverWatch`'s relay branch now calls this probe on a real `TransportState.Connected` ingress-handshake observation instead of unconditionally reporting Failure: `Success` promotes `RelayReadinessStage.END_TO_END_DATA_PLANE_OK` (the ONLY way `RelayAttemptOutcome.Success` is ever constructed) and leaves the session Connected/`RelayProtected`; `Failure` records the typed category under the full `historyPathId` and advances `attemptCombined()` to the next globally-ranked candidate - no stale Connected/RelayProtected state survives a failed probe (task L), since `_relayStage` is never advanced past `INGRESS_HANDSHAKE_OK` on that path. **Known gap**: no real server-side `/v1/relay-health`-shaped endpoint exists on any EXIT yet to answer this probe - see "Remaining condition to exercise physically" below. (D) **Path-history ownership fixed (task D)**: `VpnController`'s generic `recordConnectionOutcome`/`recordPathHistory` (in the `activeTransport.connect()` catch block - the ONLY place that could still fire for a resolved relay attempt, since the non-AWG success branch never called it) now skip entirely when `pendingAttemptContext is VpnAttemptContext.Relayed` - the single-hop write under the bare ingress endpoint id B24 documented as a known, harmless gap is now genuinely closed; `MainViewModel.recordRelayOutcome` remains the ONE writer for a relayed attempt, always under the full composite `historyPathId`. (E) **Typed persisted ingress profile (task E)**: `net.pocvpn.client.relay.IngressClientProfile` (endpoint/binding/transport-scoped, wraps the EXISTING `XrayProfile`/`XrayTlsProfile` shapes - never a third credential model - plus `profileVersion`/`issuedAt`/`expiresAt`/the real probe URL+token; excludes the ingress's own REALITY private key, the upstream exit relay uuid, and the signing key by construction - those fields simply do not exist on this type) persisted via `FileIngressProfileStore` (its own AndroidKeyStore alias/file-per-endpoint, same encrypted-at-rest discipline as `SecureXrayProfileRepository`). (F) **Real `RelayIngressResolverImpl` (task F)**: loads the endpoint-scoped profile, validates `IngressClientProfile.matches(plan)` (endpoint+binding+transport) and non-expiry (typed `PROFILE_NOT_PROVISIONED`/`PROFILE_MISMATCH`/`PROFILE_EXPIRED` `RelayFailureCategory` additions - task K), writes the matched credential into the SAME per-endpoint `XrayProfileRepository`/`XrayTlsProfileRepository` `VlessRealityTransport`/`VlessTlsTransport` already read from by endpoint id, and returns `RelayIngressResolution.Resolved(transport, kind, profile)` - never starts/owns any VPN state itself, never re-resolves the binding mid-attempt. `VpnController` gained a SEPARATE, additive `relayXrayProfileRepositoryResolver`/`relayXrayTlsProfileRepositoryResolver` pair (consulted ONLY when `pendingAttemptContext is Relayed`, and now also correctly widening `supportedKinds` for a relay-only composition root - a real gap fixed during this slice's own review, since `supportedKinds` previously gated XRAY_REALITY/TLS_TCP on the Direct-only resolvers alone) so a resolved relay's `buildTransportConfig` can genuinely reach the profile `RelayIngressResolverImpl` just wrote, without touching Direct's own germany/stockholm resolver map at all. `NotProvisionedRelayIngressResolver` remains the ONLY resolver wired into PRODUCTION today (no real ingress deployment exists to activate against) - `RelayIngressResolverImpl` is real, tested, and ready for a composition root once one does. (G) **Control-plane activation/profile endpoint (task G)**: `POST /v1/ingress-profile` (`gateway/api/handler.py`) - role-aware, additive (`ProvisioningServer.ingress_config`, `None` for every ordinary gateway deployment, zero behavior change), reusing `activations.py`'s entitlement decision and `xray_provisioning.provision_and_activate_identity` VERBATIM (no second identity/revocation/quota system for an ingress) via the new `gateway/api/ingress_activation.py` (mirrors `xray_activation.py`, renders through `xray_ingress_config_renderer.render_ingress_server_config` - upstream outbound, never freedom - instead of `xray_config_renderer.render_server_config`). Own env-var-driven config (`gateway/api/ingress_config.py`, `NOVA_INGRESS_*`, absent by default, all-or-nothing once any is set) - a genuinely separate deployment instance's config, never a field bolted onto the shared `AppConfig`. Response is client-safe only (host/port/uuid/REALITY-or-TLS public facts/profile_version/issued_at/expires_at) - never the ingress private key or upstream relay uuid. (H) **Ingress->exit relay identity provisioning contract (task H)**: `gateway/api/xray_config_renderer.render_server_config` gained an ADDITIVE `static_clients=()` parameter (empty by default - every pre-B25 caller byte-for-byte unaffected) - entries authorized on an EXIT unconditionally, deliberately NEVER cross-referenced against `activations_data`/revocation (an infra-level relay trust relationship between two deployed hosts, not a per-user entitlement), redacted in `render_server_config_redacted` exactly like any other client uuid. `gateway/tools/provision_relay_upstream_identity.py` mints the dedicated relay UUID and writes two secret-handling output files (never stdout/logs) - one for the ingress host's `NOVA_INGRESS_UPSTREAM_UUID_FILE`, one describing the `RenderedClient` fragment an EXIT operator must add to their OWN `static_clients=(...)` wiring (a deliberate, human-reviewed step this script does not automate - no live EXIT is mutated by this slice). (I) **Server deployment foundation (task I)**: `gateway/systemd/nova-xray-ingress.service` (a reviewed TEMPLATE, not installed - same pinned binary, dedicated user/config path, same hardening as `nova-xray.service`), `gateway/tools/ingress_reconcile.py` (idempotent recovery convergence, mirrors `xray_reconcile.py`), `gateway/tools/ingress_status.py` (read-only, secret-safe health/status - redacted render + last-activated-hash + active client count, never mutates anything). (J) `IngressKind.DIRECT_IP` has this real path end to end (client + control-plane); `CDN_FRONTED` remains typed-but-FOUNDATION, unchanged. **Tests**: Android - `VpnSessionHealthTest` (pure `computeSessionHealth`, Direct unaffected, every relay stage below `END_TO_END_DATA_PLANE_OK` never Protected), `VpnControllerRelayTest` (attemptContext threading, session health at the controller level, the path-history-ownership fix proven both ways - relayed skips, Direct control still records), `IngressProfileTest`/`FileIngressProfileStoreTest` (matches/expiry/JSON round-trip/endpoint isolation), `MainViewModelRelayAttemptTest`'s new probe cases (a genuine `Success` promotes `RelayProtected` and records real success; a `Failure` never shows `RelayProtected`, fails closed, and advances the combined budget; the production `NotConfiguredRelayEndToEndProbe` default never fabricates `RelayProtected` for a real ingress handshake). Gateway Python (WSL-run, `fcntl`-dependent, 284 tests total) - `test_ingress_config.py` (absent-by-default/all-or-nothing), `test_ingress_activation.py` (upstream routing never freedom, revocation converges, secrets never leak into a redacted render), `test_ingress_profile_endpoint.py` (client-safe fields only, revoked/unbound device refused, two devices never share a uuid), `test_xray_config_renderer.py`'s new `StaticClientsTests` (byte-identical with none, authorized regardless of activation status, malformed uuid fails closed, redacted). Full Android suite and full gateway Python suite both pass (one pre-existing, unrelated `test_activations.py` real-thread-race test remains environment-flaky under WSL/DrvFs `flock` timing - not touched by this slice, not a regression). **Remaining condition to exercise any of this physically, in order**: (1) a real ingress host (DIRECT_IP first) running the ingress-role `pocvpn-api`/Xray-core pair this slice built; (2) a real per-device activation against it via the new `POST /v1/ingress-profile`, populating a real `IngressProfileStore` entry on a real device; (3) a real `RelayIngressResolverImpl` wired into that device's `MainViewModel`/`VpnController` composition root (relay-scoped resolvers + context/store); (4) a REAL server-side relay-health endpoint on the EXIT for `HttpRelayEndToEndProbe` to call - this slice built the CLIENT probe and the STORAGE for its coordinates, but no server-side authenticated readiness endpoint exists yet to issue real `endToEndProbeUrl`/`endToEndProbeToken` values against; (5) the dedicated ingress->exit relay identity actually applied to a live EXIT's own activation wiring via the new `static_clients` contract (a deliberate, human-reviewed step, not automated). None of these five exist yet - no new infrastructure was purchased or deployed by this slice, per its own explicit constraint. --- HISTORY (B24) below records how this state was reached. --- Turns B23's Relayed CANDIDATE model into a real execution contract on BOTH sides - still FOUNDATION: no real RU ingress is deployed, none of this is physically exercised, **Russia hard-whitelist bypass remains UNVERIFIED**. **Supported transport matrix** (client<->ingress and ingress<->exit independent, never collapsed - reuses B23's PR #37 fix): client->INGRESS via XRAY_REALITY or TLS_TCP, INGRESS->EXIT via XRAY_REALITY or TLS_TCP - server config side real, client dial not provisioned. AWG upstream chaining is explicitly capability-not-implemented this slice (task's own "if significantly harder to implement safely, do not fake it"). **Client** (`android/.../relay/RelayExecution.kt`, `AutoGatewaySelector.kt`, `MainViewModel.kt`): `RelayedExecutionPlan` (built ONLY via `.from(RelayAttemptCandidate)`, never re-resolved mid-attempt), `RelayReadinessStage`/`RelayFailureCategory` (typed vocabulary - `INGRESS_REACHABLE`/`INGRESS_HANDSHAKE_OK`/`UPSTREAM_EXIT_HANDSHAKE_OK`/`END_TO_END_DATA_PLANE_OK`; `INGRESS_UNREACHABLE`/`INGRESS_HANDSHAKE_FAILED`/`UPSTREAM_EXIT_UNREACHABLE`/`UPSTREAM_EXIT_HANDSHAKE_FAILED`/`RELAY_AUTH_FAILED`/`END_TO_END_DATA_PLANE_FAILED`/`EXECUTION_NOT_IMPLEMENTED`), `RelayAttemptOutcome.Success` constructible ONLY at `END_TO_END_DATA_PLANE_OK` (fail-closed by TYPE, `init{}`-enforced, not convention). **PR #38 review fix, round 3 (ownership boundary, 2026-09-02, before merge)**: `RelayIngressDialer.dial()` originally returned the TERMINAL attempt outcome itself (including `Success`), which would have made a real implementation a SECOND VPN execution/state authority beside `TransportOrchestrator`/`VpnController`/the real `VpnService` - fixed before any real implementation was built on it. `RelayIngressResolver.resolve(plan)` now returns a `RelayIngressResolution` - `Resolved(transport, kind)` (a real `VpnTransport` for the client<->ingress hop, reusing the EXISTING Xray transport/service stack) or `NotProvisioned(category, detail)` - carrying NO state of its own. `MainViewModel.attemptRelayedAttempt` feeds a `Resolved` result into the SAME `TransportOrchestrator.resolve`/`VpnController.connect`/`PendingFailoverAttempt`/`armFailoverWatch` path Direct already uses - a fresh per-attempt registry naming only the resolved transport, exactly mirroring Direct. `PendingFailoverAttempt.relayPlan` lets `armFailoverWatch`'s SAME real `controller.state` observation (never this attempt's own belief) govern recording and advancement (task requirement 8): even when the resolved ingress transport genuinely reaches `TransportState.Connected` (a real handshake), this NEVER constructs `RelayAttemptOutcome.Success` - only `INGRESS_HANDSHAKE_OK` is provable from a client-only observation, never `UPSTREAM_EXIT_HANDSHAKE_OK`/`END_TO_END_DATA_PLANE_OK` (no server-side upstream readiness channel exists yet) - so it is always recorded as `Failure`. `NotProvisionedRelayIngressResolver` is the ONLY implementation wired into production, reporting `NotProvisioned(EXECUTION_NOT_IMPLEMENTED)` for every plan (no real per-ingress Xray client profile can exist without a real ingress + activation, out of scope this slice). **Named remaining gap, not closed by this fix** (would require redesigning shared, Direct-affecting `VpnController`/UI code beyond this task's scope): `VpnController`'s own pre-existing generic `recordPathHistory` also fires for a resolved relay's `controller.connect()` call, writing a separate, harmless single-hop record under the bare ingress endpoint id (never the composite `historyPathId` relay scoring reads); more importantly, `TransportState.Connected` still drives the generic `"Protected"` UI text with no relay awareness, so a FUTURE real resolver reaching genuine ingress-handshake `Connected` would show "Protected" in the UI even though this codebase's own relay bookkeeping correctly never calls it healthy - a future slice wiring a real resolver MUST teach `VpnController`/the UI layer to distinguish a relayed attempt first. `AutoGatewaySelector.AutoConnectAttempt` (sealed `DirectAttempt`/`RelayedAttempt`) + `buildCombinedAttempts`/`nextCombinedAttempt` build ONE combined, real, ranked list from `buildCandidates`+`buildRelayedCandidates` together sharing ONE `MAX_ATTEMPTS` budget - `MainViewModel.connectAuto()`/`attemptCombined()` picks its winner from THIS list and stays the ONE bounded-progression authority for the whole request: a Direct winner DIALS through the completely unchanged `TransportOrchestrator`/`VpnController`/`PendingFailoverAttempt` machinery (same pinned `GatewayAttemptCandidate.configSnapshot`, same AWG->Xray intra-gateway failover, byte-for-byte). **PR #38 review fix, round 2 (combined coordinator, 2026-09-02, before merge)**: on EITHER shape's terminal failure, control returns to `attemptCombined` with the SAME `(attempts, attemptedKeys)` state, so the next globally-ranked unattempted candidate is chosen regardless of shape - never retrying the same candidate, never substituting a different exit while keeping that candidate's own identity. An earlier version wrongly had a Direct winner delegate to a reordered DIRECT-ONLY list that then owned the rest of the Direct sub-sequence itself, silently skipping any higher-ranked Relayed candidate ranked in between and defeating the shared `MAX_ATTEMPTS` budget this row already (incorrectly) claimed - `PendingAutoGatewayContext` now carries the combined `(attempts, attemptedKeys)` shape instead, and `attemptAutoCandidate` dials exactly ONE candidate (never a list to own). Proven by `MainViewModelCombinedFailoverTest` (Direct->Relayed->Direct global order across all three ranks, Relayed->Direct order, shared MAX_ATTEMPTS bound, no candidate attempted twice, a post-relay-failure Direct success still uses the exact pinned snapshot, and Direct-only/no-ingress manifests are byte-for-byte regression-safe). `AutoGatewayDiagnostics` itself stays Direct-only (unchanged public shape); a Relayed failure surfaces only via `lastFailureReason`. **Server** (`gateway/api/xray_ingress_config_renderer.py`) reuses the EXISTING Xray machinery - no new daemon: client-facing inbound rendering/authorization is the SAME `xray_config_renderer._active_clients`/`_render_reality_inbound`/`_render_tls_inbound` functions, called verbatim, never duplicated (an ingress has no second/parallel identity system) - a revoked/expired activation is excluded exactly as for a real gateway. The new piece is the OUTBOUND: instead of `freedom` (every existing gateway config), a real authenticated VLESS connection (`UpstreamExitConfig` - REALITY or TLS, independently chosen from the inbound's own transport) to the pinned EXIT, carrying its own dedicated relay UUID credential (never a shared/global plaintext credential, never committed to this repository - always caller-supplied at render time) - "the INGRESS -> EXIT link must itself authenticate, never assume source IP == trusted ingress". Routing rules bind every client-facing inbound EXCLUSIVELY to that one upstream outbound, and NO `direct`/`freedom` outbound exists in the rendered config at all - structurally incapable of becoming an open relay or public exit by accident, not merely by convention. `render_ingress_server_config_redacted` never serializes the ingress's own REALITY private key or the upstream relay UUID. Malformed upstream inputs fail closed with `IngressConfigRenderError`. Not done this slice: no ingress systemd unit/deployment script, no ingress-specific activation HTTP endpoint, no reload wiring - genuine deployment/control-plane concerns needing a real host to design against meaningfully. `IngressKind.DIRECT_IP` has this real runtime path; `IngressKind.CDN_FRONTED` stays typed-but-FOUNDATION (no legitimate operator-controlled CDN available to validate against, and architecture principle 4 forbids ever impersonating a named third-party service to fake one). Tests: `RelayExecutionTest` (readiness/outcome fail-closed-by-construction), `AutoGatewaySelectorTest`'s new combined-attempt cases (A-F, K), `MainViewModelRelayAttemptTest` (A-D, G, K at the integration level - 7 tests, full `connect()` flow with a stub `RelayIngressResolver`, including a real `VpnTransport` fed through the real `TransportOrchestrator`/`VpnController` path), `MainViewModelCombinedFailoverTest` (round 2's own A-H combined-coordinator order proofs), `test_xray_ingress_config_renderer.py` (17 tests - L, M, transport independence, redaction, determinism). Full Android suite (97 test classes) and gateway Python suite (262 tests, run under WSL - `fcntl`-dependent) both pass. **Remaining condition to exercise any of this physically**: a real ingress host (DIRECT_IP first) running `xray_ingress_config_renderer`'s output, a real per-device activation against it issuing a real Xray client profile, and a real `RelayIngressResolver` implementation dialing it - none of which exist yet (task's own "no new infrastructure without approval"). |
| First Real DIRECT_IP Ingress Deployment Readiness | **DEPLOYMENT READY (software/repository side only)** (B26, 2026-09-02) | Closes every remaining REPOSITORY-side prerequisite named in B25's own "remaining condition" list, except the human/infra steps that genuinely require an approved real host. (A) **Production composition wiring**: `MainViewModel.Factory` now constructs and wires the REAL `FileIngressProfileStore`/`RelayIngressResolverImpl`/`HttpRelayEndToEndProbe`/per-endpoint Xray+TLS profile repository resolvers into production - `NotProvisionedRelayIngressResolver`/`NotConfiguredRelayEndToEndProbe` are no longer what a real build runs with (they remain the class-level DEFAULTS for tests/unwired callers only). A composition-root test proving this via a real Android `Context`/AndroidKeyStore was not added - this repository's Kotlin test suite has no Robolectric/instrumentation dependency today, so this remains code-reviewed, not test-proven; see this PR's own report for the exact gap. (B) **Real EXIT-side end-to-end health endpoint**: `GET /v1/relay-health` (`gateway/api/handler.py`), authenticated by a short-lived, self-verifying HMAC token (`gateway/api/relay_probe_token.py`) minted by the INGRESS at `/v1/ingress-profile` time and verified by the EXIT using only a shared secret file - never a generic `/health = 200`, never a shared live store between ingress and exit. Response is bound to the token's own signed `historyPathId` claim, matching Android's `PathCandidate.Relayed.historyPathId` format byte-for-byte. (C) **Probe token lifecycle**: short TTL (default 300s, independent of the ingress profile's own TTL), device-bound (non-reversible hash, diagnostic only - security rests on the HMAC + expiry), scoped to ONLY `/v1/relay-health` (a stolen token cannot grant VPN access), never a long-lived/global credential, never logged (only its digest). (D) **Android ingress activation client**: `ProvisioningClient.fetchIngressProfile` + `IngressProfileProvisioner` (`relay/IngressProfileProvisioner.kt`) - `MainViewModel.activateIngress(...)` is the real, callable, production-wired control-plane path (POST `/v1/ingress-profile` -> validate response matches the pinned endpoint/binding -> `IngressClientProfile` -> `FileIngressProfileStore`, endpoint-scoped). PR #40 review fix (round 2): a relayed Auto attempt hitting an activation-fixable `RelayFailureCategory` PAUSES the combined Auto sequence (stores the full pinned resume context, `MainViewModel.PendingRelayActivation` - never calls `attemptCombined`/`connect()` again while paused) and surfaces a bounded, UI-observable prompt (`MainViewModel.relayActivationNeeded`); `AppRoot` reuses the EXISTING `ActivationScreen` composable (never a manual UUID/key/token paste) to collect the ordinary activation credential. On success the BYTE-FOR-BYTE IDENTICAL paused candidate is retried exactly once (never a fresh re-ranked `connect()` - round 1's own design was itself a blocker, fixed same day); any other outcome resumes the ORIGINAL combined attempt list/budget exactly once. (E) **Refresh/expiry policy**: `IngressProfileProvisioner.ensureFreshProfile` reuses a still-valid stored profile as-is and re-activates (bounded, one attempt) only when nothing usable is stored - no unbounded retry loop. (F) **Ingress deploy/apply tooling**: `gateway/scripts/install-ingress-role.sh` (idempotent bootstrap), `gateway/systemd/pocvpn-api-ingress.service`, `gateway/privileged/nova-xray-ingress-reload` + `gateway/config/xray-ingress.env` (reusing the EXISTING `xray-activate.sh` validate/stage/atomic-replace/reload/rollback pipeline via a new `XRAY_ACTIVATE_ENV_FILE` seam, never a duplicated implementation), `gateway/tools/ingress_reconcile.py`/`ingress_status.py` (already existed, B25). (G) **EXIT relay identity apply/revoke**: `gateway/api/relay_identity_store.py` (an operator-maintained, atomically-written JSON file feeding `xray_config_renderer.render_server_config`'s existing `static_clients` parameter) + `gateway/tools/apply_relay_upstream_identity.py` (apply/revoke/status CLI) - proven idempotent and isolated from per-user `activations_data` by direct unit test (`test_relay_identity_store.py`). (H) **Deployment descriptor**: `docs/ingress_deployment_descriptor.example.json` + `docs/B26_INGRESS_DEPLOYMENT_DESCRIPTOR.md` - secret-free, explicitly never folded into the signed production manifest by this slice. (I) **Preflight/doctor**: `gateway/tools/ingress_preflight.py` - PASS/FAIL, no secret output, checks binaries/xray version/systemd units/users/file permissions/candidate-config validity/no-freedom-outbound/API port bound/clock sanity. (J) **Runbook**: `docs/B26_FIRST_INGRESS_RUNBOOK.md` - a precise, executable 20-step sequence with an explicit human-approval gate before step 7. (K) **Tests**: `gateway/api/tests/test_relay_probe_token.py`, `test_relay_health_endpoint.py` (including a real cross-process ingress-mint/exit-verify round trip), `test_relay_identity_store.py`, plus targeted additions to `test_ingress_config.py`/`test_ingress_profile_endpoint.py` - pass. The FULL gateway Python suite (322 tests, run via WSL Ubuntu on its native ext4 filesystem - this sandbox's default Windows Python lacks `fcntl`, and `/mnt/c` DrvFs flock semantics are unreliable enough to produce one spurious failure in a PRE-EXISTING, untouched concurrency test) passes cleanly: 322/322. The FULL Android suite also passes: `./gradlew compileDebugKotlin`, `testDebugUnitTest` (every pre-existing test class plus this slice's own), and `assembleDebug` all succeed (JDK 21 via Android Studio's bundled JBR - this sandbox's default JDK 26 is incompatible with the pinned Gradle 8.10 wrapper, worked around by pointing JAVA_HOME at JBR rather than upgrading Gradle). (L) **Frankfurt/Stockholm read-only suitability audit**: attempted via SSH using this session's available key; authentication failed against both hosts for every plausible username tried (`root`/`ubuntu`/`opc`/`admin`/the local account name) - see this PR's own report. Could NOT be completed without the correct SSH credential; no verdict is asserted for either host. **B31 (2026-09-03) - real SSH access obtained, architecture approved, production-readiness gap closed (software/config side only, still not deployed).** Real read-only SSH inspection of both hosts (with correct credentials this time) found: both are real, distinct, already-approved infrastructure (Germany = Oracle Cloud `vpn-poc-01`, Stockholm = AWS EC2 eu-north-1) - **repository owner approved Stockholm as the first real INGRESS role, Germany remains the pinned EXIT** (never colocating both roles on one host - that would collapse the relay to a same-host loopback hop and prove nothing, see the task's own "never a fake logical ingress" principle); both hosts' DEPLOYED backend code predates B25-B30 (neither serves `/v1/relay-health`/`/v1/ingress-profile` at all yet - current `main` already implements both handlers fully, fail-closed-by-config, so **no backend Python code changes were needed**); neither host's tracked nginx template exposed the new routes; the tracked `ingress.env.example`'s `POCVPN_API_API_PORT` default (`8443`) collided with Stockholm's own EXISTING EXIT-role `pocvpn-api.service` already bound there. Closed by this slice: `gateway/edge/nginx-pocvpn.conf` gained `location = /v1/relay-health` (Germany, GET-only, proxies to the SAME existing loopback `127.0.0.1:8443`); `gateway/edge/nginx-pocvpn-stockholm.conf` gained `location = /v1/ingress-profile` (Stockholm, POST-only, proxies to a NEW, SEPARATE loopback `127.0.0.1:8444` - the ingress role's OWN `pocvpn-api-ingress.service`, never the exit role's `8443`); `gateway/config/ingress.env.example` now defaults `POCVPN_API_API_PORT=8444` and `NOVA_INGRESS_SERVER_PORT=2093` (the approved, AWS-security-group-firewalled port for the ingress role's own client-facing Xray REALITY listener - `sg-0bb9db999a9411fc7`, TCP 2093 inbound from `0.0.0.0/0`, added by the repository owner directly); `gateway/config/api.env.example` gained the two previously-undocumented EXIT-side relay fields (`POCVPN_API_STATIC_RELAY_CLIENTS_FILE`/`POCVPN_API_RELAY_PROBE_HMAC_SECRET_FILE`, both blank by default, byte-for-byte the pre-B26 behavior until filled in); new `gateway/scripts/deploy-backend-update.sh` (backup -> rsync code copy -> Python import-sanity validation -> systemd restart -> loopback smoke test -> printed, never-automatic rollback command on any failure) closes the "no upgrade path, only fresh-install scripts" gap for moving an ALREADY-LIVE backend forward safely. Full deployment/rollback sequence and port map documented in `docs/B31_INGRESS_PRODUCTION_READINESS.md`. New tests (`gateway/api/tests/test_edge_deployment_config.py`, 24 cases) assert directly on the tracked nginx/env templates: exact routes present (never a wildcard prefix), correct loopback target per role (the ingress route specifically asserted to NEVER proxy to the exit role's own 8443), no port collisions across the whole approved port set, existing production routes byte-for-byte unchanged, no secret-shaped value in any tracked template. Full gateway Python suite re-run: 453 passed + 5 subtests (one pre-existing, unrelated `test_activations.py` thread-race flake under WSL/DrvFs, same as every prior slice - not a regression). **Nothing was deployed to either host by this slice** - this is a reviewable PR only, per the task's own explicit "do not deploy, do not merge." Still **UNVERIFIED**: Russia hard-whitelist bypass (no ingress is deployed yet; a normal unrestricted-network relay test - which is all Stockholm-to-Germany is - would not prove it either even once deployed). This row's own "DEPLOYMENT READY" claim covers ONLY the software/repository prerequisites enumerated above - it explicitly does NOT claim physical verification, which remains `docs/B26_FIRST_INGRESS_RUNBOOK.md`'s steps 7-20 plus `docs/B31_INGRESS_PRODUCTION_READINESS.md`'s own deployment procedure, both still gated on the repository owner's explicit approval to actually execute. **B31A (2026-09-04) - real device-binding blocker found live, fixed (code reviewed, NOT deployed/merged yet).** Attempting the real client-facing flow against the live Stockholm ingress found `POST /v1/ingress-profile` permanently returned `device_not_bound` for every possible device: it reused `provision_and_activate_identity`'s own CONFIRMED-device eligibility gate, which only ever became CONFIRMED via `POST /v1/activate`'s own AWG-peer-provisioning success - a flow an ingress-only host never runs (no AWG peer exists to provision, so `POCVPN_API_PROVISION_SCRIPT_PATH` is a permanent no-op there) and Android's real `activateIngress()` never calls anyway (it posts to `/v1/ingress-profile` directly). Fixed with a new `xray_provisioning.provision_and_activate_identity_selfbind` - the ingress role's OWN first-use binding authority, reusing `activations.decide_and_bind`/`finalize_reservation`/`unbind_reservation`/`per_activation_lock` VERBATIM (the same race-safe, atomic primitives `/v1/activate` itself is built from) instead of reading a CONFIRMED decision some other flow was supposed to have already made - NEVER imports or calls `provision.run_provision_peer`. A new `NOT_ELIGIBLE_DEVICE_LIMIT` outcome (403 `device_limit`) replaces `NOT_ELIGIBLE_DEVICE_NOT_BOUND` for this path's own conflicting-identity case; the regular gateway's own `/v1/activate`/`/v1/xray-profile` functions are byte-for-byte untouched. Same session also found and fixed three more live deployment-convergence gaps in `install-ingress-role.sh`/the B26 runbook (secret directory/state-directory ownership so the service can actually traverse/write them, a missing activation-global-lock init step, and the tracked systemd unit's pinned Xray-core path resolving to nothing on either real host - fixed by running the repository's own real `fetch-xray-server.sh` fetch+checksum+commit verification during install, never a manual symlink to an unpinned binary; `ingress_preflight.py` gained a matching pinned-commit check its own `_XRAY_BIN_PATH_EXPECTED_PREFIX` constant existed for but never enforced). 26 new tests (self-bind first-use/idempotency/conflict/invalid/malformed, concurrency race-safety, partial-failure rollback+recovery, role-boundary isolation from AWG, deployment-script regression guards). Full relevant suite green. **Not deployed - this PR is reviewed code only**, per this task's own explicit "do not deploy this follow-up automatically." Russia hard-whitelist remains UNVERIFIED. |
| CDN_FRONTED Ingress Support | **FOUNDATION** (B27, 2026-09-02) | Threads B23's typed `IngressKind` (`DIRECT_IP`/`CDN_FRONTED`) - a real type since B23, never consumed anywhere until now - through real candidate construction, execution, and provisioning, so both ingress strategies coexist in one ranked relay fabric. `AutoGatewaySelector.RelayAttemptCandidate`/`RelayedExecutionPlan`/`RelayActivationRequest`/`IngressClientProfile` all gained an explicit, pinned `ingressKind` field, copied verbatim end to end - never re-derived, never inferred from host/provider. Server: `/v1/ingress-profile`'s response gained an optional `ingress_kind` field (`NOVA_INGRESS_KIND`, default `direct_ip`) which `IngressProfileProvisioner.provision` cross-checks against the pinned candidate and fails closed as `Mismatched` on disagreement - the exact "frontend/origin/backend confusion must fail closed" requirement. No kind-specific branch exists anywhere in execution (`RelayIngressResolverImpl`) or health/proof (`RelayReadinessStage`/`HttpRelayEndToEndProbe`/`VpnSessionHealth` - a CDN_FRONTED session reaches `RelayProtected` through the SAME end-to-end-proof-only gate) - deliberate: CDN_FRONTED is mechanically identical to DIRECT_IP from the client's own socket-level point of view, which is exactly why almost no new execution code was needed (task requirement D's "no second VPN service or second transport core", satisfied structurally). PR #41 review fix (same day): reachability/history is NOT left unchanged - `PathCandidate.Relayed.historyPathId` (`reachability/PathCandidate.kt`) now encodes `ingressKind` explicitly (`ingressId:ingressKind:transport->exitId:exitTransport`, was missing the kind component before this fix), so the SAME endpoint+transport reclassified between DIRECT_IP and CDN_FRONTED can never silently reuse the other kind's success/failure/cooldown evidence. No pre-B27 relay history exists on any real device, so this format change orphans nothing and needed no migration code - `PathHistoryStore.get()` already treats an unmatched key as "no evidence". Never hardcodes Yandex/VK/Beeline/Selectel/a bank/any real organization as trusted infrastructure, and never impersonates a third party's traffic identity - `CDN_FRONTED` names an OPERATOR-CONTROLLED CDN/origin architecture, never a spoof (architecture principle 3, re-affirmed not weakened). Tests: `PathCandidateBuilderTest`/`AutoGatewaySelectorTest` (DIRECT_IP default when undeclared, DIRECT_IP+CDN_FRONTED candidates coexist in one ranked list each keeping its own pinned kind AND a genuinely distinct historyPathId, the SAME endpoint+transport reclassified between kinds produces a different historyPathId, stale evidence recorded under one kind's key never influences PathScorer.score for the other kind's candidate at the identical endpoint+transport), `IngressClientProfileTest` (kind mismatch fails `matches()`, CDN_FRONTED JSON round-trip), `IngressProfileProvisionerTest` (server ingress_kind mismatch rejected as Mismatched and never persisted, a kind change forces `ensureFreshProfile` to refresh rather than reuse a stale profile), `MainViewModelRelayActivationTest` (activation retry preserves the exact pinned CDN_FRONTED candidate, never silently downgraded or re-ranked), `test_ingress_config.py`/`test_ingress_profile_endpoint.py` (NOVA_INGRESS_KIND validation, a CDN_FRONTED deployment echoes that kind in its response). Full Android suite (`compileDebugKotlin`/`testDebugUnitTest`/`assembleDebug`) and full gateway Python suite (327 tests, WSL native fs - one pre-existing, unrelated thread-timing flake in `test_activations.py`, reproduced on a clean `main` checkout with zero B27 changes, confirmed not a regression) both pass. **Not done this slice**: no real CDN-fronted ingress host exists to validate against; no product UI surfaces ingress kind (Home/LocationCard stay geography-only, matching this codebase's own production-vs-debug boundary); B26's DIRECT_IP-specific deployment/preflight/runbook tooling was not extended for a CDN-fronted rollout sequence (would need a real CDN account/configuration to design meaningfully). Still **FOUNDATION** - no real ingress of either kind is deployed. Russia hard-whitelist bypass remains **UNVERIFIED**; this slice does not claim CDN whitelist bypass works, and a normal unrestricted-network relay test would not prove it either. |
| Restricted-Network Decision Authority | **FOUNDATION** (B28, 2026-09-03) | Makes real `RestrictionClass` evidence (B18/B23) decision-driving for automatic DIRECT vs RELAY path ranking for the first time, reusing the existing candidate/scoring fabric - no second decision authority, no country/provider hardcoding, no deployment or physical device dependency. **The exact rule**: `PathScorer.score` (`reachability/PathScorer.kt`) gains one new tier, `RESTRICTION_TIER = 700` (sandwiched strictly between `HISTORY_TIER=1000` and `MATURITY_TIER=200`, tier-algebra re-verified for all five tiers in the class doc). Nonzero ONLY for `RestrictionClass.POSSIBLE_HARD_WHITELIST`: `+1` for `PathCandidate.Relayed`, `-1` for `PathCandidate.Direct`, `0` for every other class (NORMAL/UNKNOWN never forces relay). The bonus depends only on candidate TYPE, never `IngressKind` - `DIRECT_IP` and `CDN_FRONTED` relayed candidates get the identical bonus, so neither ingress kind is globally preferred; their own reachability/health/history still decides ranking between them. `POSSIBLE_UDP_OR_AWG_FILTERING` gets no dedicated branch - it is itself derived from a real `awgHandshakeFresh == false` `ConnectionOutcome`, which already penalizes `AMNEZIA_WG` via the pre-existing `HEALTH_TIER` (`TransportHealthCalculator`), so a second protocol-specific branch would be redundant nested if/else. `PathScorer.isEligible`/`ineligibilityReason` gained zero new logic - restriction only affects score among already-eligible candidates, so an ineligible relay (fresh UNREACHABLE, no valid profile) can never be promoted by the bonus; when hard-whitelist evidence is suspected and no eligible relay exists, `buildCombinedAttempts` yields no `RelayedAttempt` at all and a genuinely reachable Direct path is still offered truthfully - never fabricated, never suppressed. Evidence stays distinct: endpoint reachability, transport health, real end-to-end proof (B25/B26), and restriction classification remain four separate fields feeding four separate `PathScorer` tiers, never collapsed into one boolean. **Hysteresis (TTL, explicit and time-bound)**: `RestrictionMonitor` now stamps `lastProbeEpochMillis`/`lastDiverseReachabilityEpochMillis` (real wall clock via an injectable `nowProvider`, still triggered only by a real transport-state/network-type transition, never a timer); `RestrictionEvidence` gained matching optional `gatewayProbeEpochMillis`/`diverseProbeEpochMillis` fields; `RestrictionClassifier.classify` gained `nowEpochMillis`/`staleAfterMillis` params (default 30 minutes, the same window `ReachabilityEngine` already uses) - once a probe's own timestamp is stale (or future-dated), its value is treated as unknown rather than trusted indefinitely, decaying `POSSIBLE_HARD_WHITELIST`/`POSSIBLE_UDP_OR_AWG_FILTERING` back toward `UNKNOWN`. Backward compatible: an undated value (every pre-B28 caller) is legacy-trusted as-is, so no existing classification changed. **Diagnostics**: `MainViewModel.combinedAutoRankingDiagnostics()` (new) maps the already-public, already-ranked `combinedAutoAttempts()` output into `{restrictionClass, ranked: [{kind: "DIRECT"|"CHAIN_DIRECT"|"CHAIN_CDN", score, reasons}]}` - reasons filtered to the stable `PathScorer.Reason` token vocabulary only (no endpoint host/IP, UUID, activation credential, probe token, or key ever appears); the existing Direct-only `AutoGatewayDiagnostics` (B16) is untouched. B26/B27 guarantees (pinned ingress endpoint/kind/bindings/transports, kind-aware `historyPathId`, activation pause/resume of the exact candidate, no re-ranking after activation, bounded retry, atomic single-owner activation claim) are structurally unaffected - restriction scoring runs strictly before ranking/pinning and has no further effect once a candidate is activated. **Tests**: `RestrictionClassifierTest` (staleness/hysteresis, undated-legacy-trust, future-dated clock-skew), `PathScorerTest` (NORMAL/UNKNOWN never force relay, POSSIBLE_HARD_WHITELIST makes an eligible relay outrank direct, both ingress kinds get the identical bonus, an ineligible relay never wins merely from suspected whitelisting, POSSIBLE_UDP_OR_AWG_FILTERING has no dedicated branch, worst-case tier-dominance swings), `AutoGatewaySelectorTest` (end-to-end through the real `buildCombinedAttempts` pipeline: both DIRECT_IP and CDN_FRONTED relayed attempts outrank direct under hard-whitelist evidence with neither kind preferred, no-eligible-relay fails closed), `RestrictionMonitorTest` (real probe stamps/overwrites the injected-clock timestamp), `MainViewModelAutoGatewayTest` (no-secret-leakage proof for the new diagnostics surface) - all pass, alongside the full existing suite (`compileDebugKotlin`/`testDebugUnitTest`/`assembleDebug` all green, zero regressions). **Not done this slice**: no physical validation on any real restrictive network (still FOUNDATION-only, same caveat as B23/B24/B25); `RestrictionClass` is still never inferred from country/ASN/provider identity anywhere in this codebase; no new UI surface renders the new diagnostics (diagnostics-only); the gateway/Python control plane is completely untouched (Android/Kotlin reachability-decision logic only). **PR #42 review fix (2026-09-03)**: two blockers fixed, same single decision authority. (1) No-viable-relay semantics corrected - `AutoGatewaySelector.buildCombinedAttempts` gained a `restrictionClass` parameter and one inline gate: under `POSSIBLE_HARD_WHITELIST` with zero eligible relay candidates, EVERY Direct attempt is excluded (never a silent fallback merely because one endpoint's own probe says reachable) - the new `isRestrictedNetworkExhaustion()` helper and `VpnError.RestrictedNetworkNoViableRelay` report this truthfully; when an eligible relay exists, Direct is not excluded, only outranked. (2) Real hysteresis added - `RestrictionStabilizer` (new, pure, O(1) state) applies a 90-second minimum-residence hold before a raw classification change is promoted to decision-driving, so two back-to-back fresh-but-differing probe results can no longer flip DIRECT/RELAY ranking; `NO_NETWORK`/`CAPTIVE_PORTAL` stay immediate. `MainViewModel.stabilizedRestrictionClass()` (new) feeds ONLY `buildCombinedAutoAttempts()`; the existing `restrictionClass()` stays raw/immediate for every other consumer. Full test suite re-run green. **PR #42 review fix #2 (2026-09-03) - final diagnostics blocker**: `combinedAutoRankingDiagnostics()` could report a different `RestrictionClass` than the one that actually drove the ranking it described (raw `restrictionClass()` vs the stabilized value `combinedAutoAttempts()` actually used internally, disagreeing during the hold window). Fixed with one shared internal snapshot - `buildCombinedAutoRankingSnapshot()` calls `stabilizedRestrictionClass()` exactly once and returns `{restrictionClass, attempts}` together; `connectAuto()`, `combinedAutoAttempts()`, and `combinedAutoRankingDiagnostics()` all project from that one call, never a second independent read. A narrow `nowProvider` test seam was added to `MainViewModel` (defaults to the real wall clock, same additive pattern as `RestrictionMonitor`'s own) to prove this deterministically through the real evidence pipeline without a real 90-second sleep. Full test suite re-run green. **Russia hard-whitelist bypass remains UNVERIFIED.** |
| Field Diagnostics / Support Bundle | **FOUNDATION** (B29, 2026-09-03) | Automatic, sanitized, structured connection diagnostics for remote support - no developer mode/ADB/logcat required from the tester. **Model**: `DiagnosticSession` (session id, app version, coarse network facts, `rawRestrictionClass`/`stabilizedRestrictionClass`, routing/gateway-selection mode, `selectedPathKind` - `DIRECT`/`CHAIN_DIRECT`/`CHAIN_CDN`/`PRIVATE`/`NONE`, selected transport, a bounded `events` timeline capped at 200, outcome, failure reason) - no endpoint host/IP, only enum labels and the existing coarse `NetworkFingerprinter` id. `DiagnosticEventType` (19 categories) is a labeling vocabulary over this codebase's own real state (`RestrictionClassifier`/`RestrictionStabilizer`, `AutoGatewaySelector`, `VpnSessionHealth`, `RelayReadinessStage`, `IngressActivationOutcome`) - never a second connection state machine. `DiagnosticFailureReason` (17 categories) is populated by pure mapping functions from the existing `VpnError`/`RelayFailureCategory`/`IngressActivationOutcome`/`RestrictionClass` types, never replacing them. **Automatic capture**: `SupportDiagnosticsRecorder` is the one place a session is assembled - narrow, typed `record*`/`finish*` functions (every `record*` function has zero raw `String` parameters, no exception, reflection-proven; PR #43 review fix removed a one-method exception for `recordManifestSourceSelected`, which now takes the closed `ManifestSourceKind` enum instead of a free-text label); non-terminal `record*` calls append to a still-open session (a multi-candidate Auto failover sequence stays ONE session/timeline), terminal `finish*` calls close it (idempotent - a generic backstop and a precise typed call can never double-record). Wired into `MainViewModel` as an additive nullable constructor param at `connectAuto`/`connectManual`/`connectPrivate`/`attemptCombined`/`attemptRelayedAttempt`/`armFailoverWatch`, plus a generic `sessionHealth`-StateFlow collector backstop that guarantees no session is ever left open forever. `InMemoryDiagnosticSessionStore` bounds retention to the last 8 sessions, oldest-first eviction - no unlimited logging. **Snapshot consistency**: `connectAuto`'s raw/stabilized restriction fields come from the SAME `CombinedAutoRankingSnapshot` (B28) that decided the ranked attempts, never independently recomputed. **Sanitization**: two layers - structural (the recorder's API cannot accept a raw string at all) plus a pattern-based defense-in-depth pass (`DiagnosticSanitizer`) applied a second time at export, rejecting UUID-shaped strings, long mixed-case Base64 key-shaped blobs, bearer auth headers, PEM blocks, credential key-value pairs, URLs with query data, and IPv4/IPv6 addresses - replaced with a fixed `[redacted]` marker, never a partial echo. Proven with deliberately-planted secret sentinel strings (UUID credential, real-shaped AWG key, bearer token, PEM block, URL with secret query param, endpoint IP) confirmed absent from the exported JSON. **Bundle/export/UI**: `buildSupportBundle`/`toJson()` (deterministic, fixed key order); `MainViewModel.exportSupportBundleJson()`/`recentDiagnosticSessions()`/`clearDiagnosticSessions()`/`lastConnectionResultSummary()`; `SettingsScreen`'s new "Diagnostics" section shows only a simple human-readable last-result sentence with "Export diagnostics"/"Clear diagnostics" one tap away; export triggers a real Android `ACTION_SEND` share-sheet intent - never an automatic/silent upload to any backend (no telemetry service exists or was added this slice). **Tests**: `DiagnosticSanitizerTest`, `DiagnosticSessionStoreTest`, `SupportDiagnosticsRecorderTest` (DIRECT/CHAIN_DIRECT/CHAIN_CDN success, RestrictedNetworkNoViableRelay, transport/data-plane/relay-proof/activation/control-plane failures, raw-vs-stabilized consistency, last-attempt-wins recording, bounded events, abandon-on-supersede), `SupportBundleTest`, `DiagnosticTypesTest`, and `MainViewModelSupportDiagnosticsTest` (a real successful manual connect end to end; the exported bundle never leaks a real gateway config's own host/public key/tunnel IP). All new tests plus the full existing suite green (1148 total), `compileDebugKotlin`/`assembleDebug` green. **Not done this slice**: no field-testing with a real user/support workflow (stays FOUNDATION-only until that happens); no backend telemetry/upload endpoint exists or was added (export is local share-sheet only); not every single MainViewModel failure branch carries an individually-instrumented precise typed reason (the generic sessionHealth backstop covers the remainder); no persistent (disk-backed) session store yet - sessions do not survive app process death. This slice does NOT prove Russia whitelist bypass - restrictive-network behavior remains UNVERIFIED. **B30A physical-validation fix (2026-09-03)**: real Oppo CPH2173 hardware testing found `SettingsScreen`'s root `Column` had no scroll container, so on real devices (confirmed reproducible even at the OS's minimum Display size/Font size) the Smart Connect explanation and this entire Diagnostics section - including Export/Clear diagnostics - were composed but positioned entirely below the visible viewport, unreachable by touch; fixed with `Modifier.verticalScroll(rememberScrollState())`. Same physical pass also found system Back while Settings/AppSelector was open fell through to Activity-finish (exiting to the launcher) instead of returning to Home, since nothing was ever pushed onto a real back stack for the in-file `settingsRoute` state; fixed with a `BackHandler` driving the same `settingsRoute` authority via a new pure `settingsBackTarget` function. Both fixes re-verified on the same physical device. This does not change B29's FOUNDATION status or the Russia-whitelist UNVERIFIED status above. **B30C physical-validation fix (2026-09-03)**: real Oppo CPH2173 hardware testing found B29's own session lifecycle closed a session the moment the VPN first reached PROTECTED, so a LATER real reconnect/failure incident on an already-protected session (real network loss -> `VpnController.handleNetworkLost()`/`reconnectLoop()` -> reconnect exhaustion, B30B's own now-correctly-firing signal) had no open session left to record into at all - `SupportDiagnosticsRecorder.finishFailed()` silently no-op'd on its own `open ?: return` guard, so the support bundle carried only the original PROTECTED session, never the mid-session incident. Fixed narrowly: `MainViewModel`'s existing `sessionHealth` collector backstop gained one new branch, on `VpnSessionHealth.Reconnecting` - reached ONLY via a real post-Connected network loss, never during an initial connect attempt - opens a NEW session (same `startSession`/`finish*` lifecycle every other session already uses) exactly when no session is currently open (`currentSessionId() == null`, the single-authority dedup - true once per incident regardless of how many backoff attempts fire), labeled with one new event `DiagnosticEventType.RECONNECT_INCIDENT_STARTED`. No new outcome vocabulary, no new observer, no architecture change - the incident session still terminates PROTECTED/FAILED/DISCONNECTED through the SAME `finishProtected`/`finishFailed`/`finishDisconnected` calls. Physically re-verified end to end on the same device: initial connect -> PROTECTED (Germany/XRAY_REALITY) closed the original session; total connectivity loss (WiFi+mobile data disabled) drove the app through "Reconnecting..." to "Connection failed" (reconnect exhaustion); the real exported support bundle (via Settings -> Export diagnostics -> real Android share sheet) showed exactly 2 sessions - the original PROTECTED session untouched, plus a NEW, separate FAILED session carrying `RECONNECT_INCIDENT_STARTED` and `failureReason: GATEWAY_UNREACHABLE`; connectivity restored, manual Retry produced a third, separate PROTECTED session (Sweden) - no dangling session, no duplicate-per-attempt session. Sanitization re-confirmed PASS on both real exported bundles (no host/IP/URL/bearer token; the only UUID-shaped string present was the session's own opaque grouping id, which this file's own docs already document as safe to display verbatim). Does not change B29/B30's FOUNDATION status; does not touch the B29 diagnostic-session-at-initial-PROTECTED behavior itself, ingress, B21/B22, transport ranking, or reconnect policy; the Russia hard-whitelist status above remains UNVERIFIED. |
| Resilient Activation & Control-Plane Access | **FOUNDATION** (B30, 2026-09-03) | Makes activation/profile-retrieval network calls bounded, typed, and origin-list-driven, without a second trust system. **Trusted origin model**: `ControlPlaneOrigin(gatewayId, host)`, built ONLY by `ControlPlaneOriginSetBuilder.forGateway(gatewayId)` from `ProductionGatewayCatalog` (the same compiled trusted gateway facts everything else in this codebase already uses) - never a caller-suppliable URL. Today's catalog carries one physical origin per gateway; the list shape is what makes the mechanism genuinely N-origin-capable the moment a second trusted origin exists, no call-site changes needed then. **Generic executor**: `TrustedOriginRequestExecutor` (pure, synchronous, no I/O) tries each origin at most once, in order, bounded by `origins.size` - never unbounded, stops early on `AUTHORIZATION_REJECTED` (a rejected credential is never retried against a different origin). Reused by `ActivationResilienceCoordinator` (activation) and `IngressProfileProvisioner` (ingress-profile diagnostics), never transport-specific copies. **Failure taxonomy**: `ControlPlaneFailureReason` (DNS/timeout/TLS/HTTP-unavailable/authorization-rejected/malformed/trust-rejected/untrusted-redirect/all-origins-exhausted), mapped into eight new `DiagnosticFailureReason.CONTROL_PLANE_*` values for B29's support bundle. **Redirect lock-down**: `ProvisioningClient.executeGeneric` (shared by all five endpoints - peers/activate/xray-profile/xray-tls-profile/ingress-profile) now sets `instanceFollowRedirects = false` - a real audit finding: `HttpsURLConnection` followed redirects transparently by default, before any status-code check ran, which would have silently resent the bearer credential to whatever host a response named. **Activation resilience**: `ActivationResilienceCoordinator` wraps `ProvisioningClient.activate` with the executor's bounded/typed discipline, skips the network via a caller-supplied `hasValidLocalActivation` check, and is idempotent by construction (never rotates identity - the caller's already-get-or-created public key is always reused, matching the server's own credential-digest-locked idempotent activation in `gateway/api/activations.py`) - never applies/persists anything itself. **PR #44 review fix**: `MainViewModel.activateDevice`'s real network call now genuinely routes through `ActivationResilienceCoordinator` via a new `controlPlaneOriginsForActivation` constructor seam (defaults to the real `ControlPlaneOriginSetBuilder::forGateway`) - the ~250-line success/persistence block (gateway-identity cross-check, config/profile/identity writes, Xray/TLS provisioning) is untouched, now fed whichever origin's result actually succeeded; `retryActivation()` already called `activateDevice` verbatim so Retry automatically reuses the resilient path. **Trusted-origin audit performed**: decoded the embedded signed manifest directly - each gateway's endpoint carries exactly one host across all transport bindings (different ports/protocols, not alternate hosts); audited `gateway/api/activations.py` - activation state is a per-VPS-local store, Germany and Stockholm are confirmed NOT interchangeable activation backends. Conclusion: no genuine second trusted control-plane origin exists today for either gateway - `ControlPlaneOriginSetBuilder` was left unchanged rather than inventing one, per the review's own accepted "report the architecture blocker" alternative; the fallback mechanism itself is proven correct through the real `activateDevice()` path via the new test seam, injecting a synthetic two-origin list. **Xray/TLS profile fetch now also genuinely wired through the executor** (`XrayProfileProvisioner`/`XrayTlsProfileProvisioner` gained additive nullable `gatewayId`/`diagnosticsRecorder` params, wired for all four Factory instances) via a new `fetchThroughTrustedOrigins` helper; `IngressProfileProvisioner` deliberately stays diagnostics-instrumented only (it's `EndpointId`-scoped, not `ProductionGatewayId`-scoped, so the same origin model would misrepresent its trust). **New tests**: `TrustedOriginProfileFetchTest`, `MainViewModelActivationGatewayIdentityTest` additions (primary-fails/secondary-succeeds applied+persisted through the real flow, malformed-primary-leaves-no-partial-state, all-origins-exhausted friendly failure, authorization-rejection stops fallback), `MainViewModelSupportDiagnosticsTest` addition (real activateDevice diagnostics carry no host/IP/URL/credential). All new + full existing suite green (1190 total, same one pre-existing unrelated `EffectiveConfigDiffTest` failure); `compileDebugKotlin`/`testDebugUnitTest`/`assembleDebug` all re-verified green. **PR #44 review fix, round 2 (origin-discarding blocker)**: every gateway-scoped network seam is now genuinely origin-aware - `activationClient`/`stockholmActivationClient` and `XrayProfileProvisioner`/`XrayTlsProfileProvisioner`'s `fetchXrayProfile`/`fetchXrayTlsProfile` all changed from `(publicKey, credential) -> Result` to `(origin, publicKey, credential) -> Result`, each defaulting to `{ origin, pk, cred -> ProvisioningClient.xxx(pk, cred, origin.host) }` - round 1's wiring wrapped the calls in the executor structurally but every actual callback still discarded `origin` and re-dialed the SAME pre-bound host, so a 2-origin executor was retrying, not failing over. `XrayProfileProvisioner`/`XrayTlsProfileProvisioner`'s `gatewayId` is now non-null (defaults to GERMANY) so `provision()` always routes through the origin-aware path, no parallel non-origin-aware branch. New tests prove HOST-LEVEL routing directly (not just call count): `MainViewModelActivationGatewayIdentityTest` uses real distinct hostnames (`"primary.example"`/`"secondary.example"`) and asserts the exact sequence of hosts the fake network function was actually called with. All new + full existing suite green (1190 total, same one pre-existing unrelated failure); `compileDebugKotlin`/`testDebugUnitTest`/`assembleDebug` re-verified green. **Still not done**: real production data has only one physical origin per gateway (see audit above) so cross-HOST fallback isn't exercised by live traffic yet, only proven via the test seam; no live-HTTPS integration test for the redirect lock-down; this does NOT prove Russia hard-whitelist bypass, which remains UNVERIFIED. |
| Real CDN-Fronted Ingress (Production, Provider-Agnostic) | **FOUNDATION** | **B35 (2026-09-05) - approved post-B31 product direction, repository owner.** Extends the existing `IngressKind.CDN_FRONTED` type (B23/B27, still FOUNDATION-only - see the CDN_FRONTED Ingress Support row above) toward an actual physically-deployed CDN-fronted ingress: `client -> CDN edge -> origin ingress -> Xray -> EXIT -> Internet`, kept structurally distinct from `DIRECT` (no relay) and `CHAIN_DIRECT` (today's real Stockholm relay, B31/B31A/B32 - direct-IP ingress, no CDN). B35 foundation code is now merged: signed provider-profile v2, client capability gating, Android XHTTP provisioning/resolver/renderer foundation, server XHTTP provisioning/rendering, a loopback-only Xray origin backend, and a fail-closed nginx origin template all exist. The deployment preflight is tracked in `docs/B35_CDN_DEPLOYMENT_PREFLIGHT.md`. No real external CDN provider/origin chain is deployed or data-plane verified yet. **Truth boundary (repeated deliberately, not assumed once and forgotten): a Russian IP, provider, ASN, or CDN brand alone never proves hard-whitelist reachability - CHAIN_CDN may only be called VERIFIED after a real deployed CDN ingress is tested end-to-end from a real restricted Russian network** (see the Field-Validation Truth Boundary row below). **Implementation order (repository owner-approved, non-binding sequencing only)**: (A) this row, (B) CDN Provider Capability Profile, (C) origin nginx/Xray path, (D) CDN data-plane proof, (E) Android provisioning/capability gating, (F) real CHAIN_CDN candidate construction, (G) Smart Connect integration, (H) CDN diagnostics, (I) a second provider/ASN-diverse candidate, (J) restricted Russian mobile-network field test. REALITY port 443/SNI rotation research (P1, see below) is explicitly parallel research, not a blocker for this sequence. |
| CDN Provider Capability Profile | **FOUNDATION** | **B35.** A typed, provider-specific capability model (provider, ASN, `clientFacingHostname`, `cdnTechnicalHostname`, `originHostname`, `originTlsServerName`, XHTTP mode/path/`uplinkHTTPMethod`/extra params, padding/query behavior, header behavior, TLS requirements, SNI/fingerprint compatibility, Host-header/origin behavior, caching requirements, timeout/streaming/request-body constraints, supported exits, minimum client/Xray-core capability, health/reachability state) - extends the existing `IngressKind`/`EndpointTransportBinding.metadata` extensibility pattern (B23) rather than a new manifest binary field, so no re-signing ceremony is required merely to add this type. No vendor's behavior may be hardcoded into generic routing/candidate logic (`AutoGatewaySelector`, `PathScorer`, `RelayIngressResolverImpl` stay provider-agnostic, per architecture principles 4/7). Typed model and versioned metadata codec now exist with bounded validation, explicit host identities, XHTTP/TLS/request constraints, exit and client/core requirements, plus a fail-closed client/core compatibility gate used by `AutoGatewaySelector` for `CDN_FRONTED` relay candidates. Existing signed manifest encoding is unchanged. This is still foundation: live runtime capability sourcing, live health integration, Xray configuration, provider deployment and end-to-end validation remain outstanding. See `docs/CDN_PROVIDER_PROFILE.md`. |
| Multi-Provider / Multi-ASN CDN Ingress Pool | PLANNED | **B35.** Extends architecture principle 7 (multi-provider/ASN gateway infrastructure) to CDN ingress specifically: multiple CDN ingress candidates, never a static assumption that Yandex/VK/Beeline/Selectel or any other provider is universally reachable - availability measured per network/operator/region via the same `ReachabilityEngine`/`PathScorer` evidence model already governing `DIRECT_IP` relays (B18/B19/B28), never a second scoring authority. No code exists yet; depends on the two rows above. |
| XHTTP Traffic-Shape / CDN Compatibility | **FOUNDATION / RESEARCH** | **B35.** First-class compatibility area tracking upstream Xray-core PR #5414 (adds configurable XHTTP behavior relevant to Russian CDN filtering) - not yet vendored/pinned in this repository's `gateway/xray/VERSION`-pinned core. Requirements: provider-specific `uplinkHTTPMethod` (never a global "Russia = GET"/"always PUT" rule), configurable padding/query behavior, configurable safe header behavior, reviewable fingerprintable URL/request patterns, realistic/compatible User-Agent policy where applicable, compatibility tested per CDN provider, and no claim that POST/GET/PUT/HEAD universally work across Russian providers. Configuration should eventually be distributable through the existing signed manifest/bootstrap architecture (B12/B17) rather than requiring a new APK release - see the Signed CDN Lifecycle row below. B35 now pins Xray-core v26.7.28 plus the tracked bounded-dial XHTTP patch and implements the first executable `packet-up + POST` resolver/renderer contract. Provider-specific traffic-shape compatibility remains RESEARCH/UNVERIFIED; every provider change still requires real end-to-end validation, never a copied community claim. |
| CDN Origin Model & Data-Plane Proof | **B35 OPEN PR: unrestricted-network data plane VERIFIED; restricted-network behavior UNVERIFIED** | The model keeps client-facing CDN, origin, XHTTP path, ingress, relay and exit identities distinct; only full END_TO_END_DATA_PLANE_OK proves a usable path. B35's isolated Stockholm XHTTP ingress uses Cloudflare, nginx and loopback Xray, then relays to Frankfurt EXIT. The 2026-09-14 physical Android debug-only Manual(XRAY_XHTTP) pass carried normal browser traffic through Cloudflare -> Stockholm XHTTP -> Frankfurt EXIT -> Internet and observed exit IP 152.70.43.1 (see docs/B35_STOCKHOLM_XHTTP_ROLLOUT_2026-09-14.md). DNS/TCP/TLS or a health response alone do not prove this. This evidence does not establish automatic CDN selection or Russia/restricted-network reachability. |
| Smart Connect CHAIN_CDN Authority | **FOUNDATION** | B28's RestrictionMonitor/PathScorer ranking is ingress-kind-neutral and can score CHAIN_CDN without provider-name logic. B35 now supplies a real signed CDN ingress/profile and an executable Android XHTTP path, superseding the historical no-candidate statement. Physical B35 proof used a debug-only Manual(XRAY_XHTTP) pin; automatic CHAIN_CDN selection and failover on a restrictive network remain unverified. Eligibility must remain fresh-evidence-gated and fail closed for protected traffic. |
| CDN Diagnostics (typed CDN/XHTTP failures) | **PARTIAL / B36** | Extends the existing B29 `DiagnosticFailureReason` vocabulary and `SupportDiagnosticsRecorder`/support-bundle path without another state machine. A pinned `CHAIN_CDN` + `XRAY_XHTTP` attempt now maps ingress handshake failure to `XHTTP_HANDSHAKE_FAILURE`, the existing out-of-band relay probe failure to `RELAY_PROOF_FAILURE`, and Xray-native initial remote-confirmation or post-Connected relay-health watchdog terminal failure to `DATA_PLANE_PROOF_FAILURE`. Direct/AWG/REALITY/TLS mappings remain unchanged. The closed vocabulary also names `CDN_DNS_FAILURE`, `CDN_TLS_FAILURE`, `CDN_METHOD_REJECTED`, `CDN_RATE_LIMITED`, `CDN_TIMEOUT`, `CDN_ORIGIN_UNREACHABLE`, `ORIGIN_TLS_FAILURE`, and `ORIGIN_PROXY_FAILURE`, but B35 supplies no typed data-plane HTTP/DNS/origin signals that distinguish those causes, so B36 does **not** emit them or infer them from exception strings. Existing B29 sanitization/export rules remain in force; no telemetry upload. Russia/restricted-network validation remains **UNVERIFIED**. |
| Signed CDN Lifecycle / Emergency Rotation | PLANNED | **B35.** CDN ingress endpoints/providers/capability profiles must eventually be addable/disabled/rotated/capability-updated through the existing trusted signed manifest/bootstrap architecture (B12/B17/B20 multi-origin fetch) where appropriate, rather than requiring a new APK release per endpoint/provider change - extends the existing `EndpointManifest`/`EndpointTransportBinding` model, never a second distribution channel. No code exists yet; no signing ceremony performed by this entry. |
| Client Capability Gating & TLS/Fingerprint Compatibility Policy | **FOUNDATION** | **B35.** Because XHTTP behavior (see that row above) depends on newer Xray-core/client capability, the app must track minimum supported runtime capability/version and never present a CHAIN_CDN candidate to a client that cannot execute its required XHTTP options - failing explicitly rather than silently downgrading to an incompatible config (extends the existing `xrayAvailableFor`/`xrayTlsAvailableFor` device-capability-gating precedent, B17-2). `CdnClientRuntimeCapabilities` and `CdnClientCapabilityPolicy` now check supported exit id, minimum client version, minimum Xray-core version, required client capability labels, XHTTP mode/method/padding support, TLS version/fingerprint/ALPN support, streaming support, and request body/timeout bounds. `AutoGatewaySelector.buildRelayedCandidates()` applies this gate only to `IngressKind.CDN_FRONTED`; missing/malformed/unsupported/incompatible CDN profiles are excluded before scoring, while `DIRECT_IP` and legacy/null ingress-kind bindings keep existing behavior. Still outstanding: real runtime capability values must be wired from the shipped app and pinned core, Xray config generation must consume the compatible profile, provider deployment must be validated, and TLS/fingerprint choices must be audited against the actual vendored `gateway/xray/VERSION` core. A fingerprint setting alone is never claimed to provide censorship resistance. |
| Nontechnical Russian User UX (reaffirmed product requirement) | FOUNDATION (existing UX pattern already satisfies this; reaffirmed as binding for every CDN/anti-censorship row above) | **B35.** A user must never need to manually enter or understand UUID/private key/profile/gateway/CDN provider/transport/SNI/fingerprint/crypto settings/routing rules. Target UX stays Activate -> Connect -> optionally Send diagnostics; the app performs network classification, reachability checks, candidate selection, transport/path selection, failover, and safe routing itself (already true of `RestrictionClassifier`/`AutoGatewaySelector`/`SmartConnectDecisionEngine`/`RoutingDecisionEngine`, B16-B19/B28) and diagnostics stay sanitized, consent-gated, share-sheet-based (B29) - no logcat/manual technical troubleshooting requirement for CDN/XHTTP internals either, once built. |
| Field-Validation Truth Boundary (CDN / Hard-Whitelist) | UNVERIFIED (policy boundary, not a feature) | **B35.** Explicit claims boundary for every CDN row above, reaffirming architecture principle 10: Russia hard-whitelist bypass remains UNVERIFIED until tested on a real restricted Russian mobile network; `CDN_FRONTED` must not be called IMPLEMENTED merely because types/models exist (already true per the CDN_FRONTED Ingress Support row); a Russian IP/cloud provider/CDN brand alone never proves reachability; no provider may be claimed to work on all operators/regions without evidence; Wi-Fi success does not prove mobile-network success; end-to-end data-plane proof (CDN Origin Model row) is the sole authority for any reachability claim. |
| multi-provider gateway infrastructure | PLANNED | **B13 (2026-08-30) update**: a real second provider IS now deployed - Stockholm (AWS, eu-north-1) alongside Germany (Oracle Cloud), different provider AND different ASN, with AWG/REALITY/TLS_TCP all physically verified on both (see the Gateway Pool row above for the full evidence trail). `EndpointDescriptor.provider`/`.asn` (B11) are populated for both real endpoints, not just typed placeholders. Kept PLANNED, not IMPLEMENTED, because the capability this row names is the full multi-provider PRODUCT capability, and separates FOUR distinct things that must not be collapsed into one status: (1) **physically deployed gateway data planes** - DONE for both (AWG+REALITY+TLS_TCP, see Gateway Pool row); (2) **per-device endpoint provisioning availability** - real and evidence-based for Germany (`ClientTunnelIdentityStore`, migrated only from a genuine prior activation); Stockholm's AWG client tunnel identity is NOT provisioned on any device today, including the one physically tested, and stays that way until a real control-plane request actually succeeds - `GatewayPickerDialog` truthfully shows it disabled until that happens, it is NOT self-service/user-usable for an arbitrary device yet. **B14 (2026-08-31) update**: the self-service PROVISIONING CODE PATH for Stockholm is now real and complete on the client side - `MainViewModel.activateDevice(credential, targetGatewayId)` takes an explicit target, `ProvisioningClient.activate/fetchXrayProfile/fetchXrayTlsProfile` now have endpoint-aware 3-arg overloads that POST to the REQUESTED gateway's own edge (`ProductionGatewayCatalog.STOCKHOLM.awg.endpointHost`, `16.170.208.231`, for Stockholm) rather than always Germany's, the response is required to unambiguously match the REQUESTED gateway specifically (`matchGatewayId`, now including `gatewayTunnelIp` - a cross-endpoint or mismatched response is rejected outright, never applied), and a successful match writes ONLY that gateway's `ClientTunnelIdentityStore` entry and provisions ONLY that gateway's own endpoint-scoped `XrayProfileRepository`/`XrayTlsProfileRepository` (Stockholm gets its own provisioner pair, wired to its own repositories - never Germany's, never `XrayDiagnosticsActivity`, which stays debug-build-only). **This is "self-service provisioning implemented in code", not "self-service provisioning live"** - see (3); (3) **control-plane availability** - real and deployed for Germany (`/v1/activate` on `152.70.43.1`); **B15 (2026-08-31) update: a real `pocvpn-api` instance is now deployed and live at Stockholm's edge (`16.170.208.231`)**, configured exclusively from Stockholm's own facts (own AWG keypair/tunnel IP, own REALITY keypair, own TLS cert), reachable via the client-side code path (2) unchanged. Verified live via the real HTTP control-plane path with synthetic device credentials: `POST /v1/activate` (real AWG peer creation), `POST /v1/xray-profile` for both `reality` and `tls` transports (real per-device VLESS identity issuance, real Xray reload/publish), and fail-closed auth (no/wrong bearer credential -> 401, non-POST -> 403, unknown route -> 404, mismatched gateway facts -> client-side rejection per `matchGatewayId`, unchanged). **B15 (2026-08-31, completed)**: a real physical device attempt found and fixed a missing UI entry point (see Gateway Pool row), then found and the operator fixed a real AWS security-group gap (port 443 had no inbound rule), then completed a real on-device activation end to end - `ClientTunnelIdentityStore[STOCKHOLM]` is now genuinely populated from a real activation response on the tested device, `GatewayPickerDialog` shows Stockholm as a normal selectable row, and this survived a full app restart. Germany unaffected. Germany's control-plane/credentials/AWG peers were not touched by this deployment (separate VPS, separate `/etc/pocvpn/api.env`, separate `ClientTunnelIdentityStore` entry); (4) **automatic multi-gateway selection/failover** - **B16 (2026-09-01) update - superseded**: now real, pure decision logic (`AutoGatewaySelector`), live-wired behind a device-local Auto toggle, and physically validated on a real device (real Auto failover from Germany to Stockholm confirmed, real `16.170.208.231` data-plane connection) - see the `automatic gateway selection / failover` row below for the full detail. Manual selection (`SelectedGatewayStore`/`SelectedProductionGatewaySource`, `SmartConnectCandidateSelector` picking a TRANSPORT within whichever gateway is selected) remains the default and is unaffected. (5) **addressing stability** - Stockholm's `16.170.208.231` is an auto-assigned AWS public IPv4, not a purchased/reserved Elastic IP; treat this deployment as B13 validation/foundation infrastructure, not durable production addressing, until stable addressing is a deliberately approved separate change (see Gateway Pool row's own CURRENT STATE (F)). |
| Private Gateway Mode | **IMPLEMENTED** (B22, 2026-09-02 - physically validated) | Real code, real tests, wired into the real connect path - see `PROJECT_ARCHITECTURE.md`'s own "Private Gateway Mode (B22)" section for the full design (`GatewaySelectionMode` explicit three-way authority, `PrivateGatewayConfig`/`PrivateGatewayConfigValidator`/`FilePrivateGatewayStore`, a dedicated `PrivateGatewayKeyRepositoryFactory` reusing the existing AndroidKeyStore-encrypted identity architecture, converging into the SAME `GatewayConfigSnapshot`/`VpnController`/`AmneziaWgTransport` pipeline via one additive field on the existing B16 pinning mechanism - no second execution stack). Real, non-debug-only UI (`PrivateGatewayDialog`, reached from `GatewayPickerDialog`'s new row). First-slice scope: exactly one private gateway, AWG-only, no Smart Connect/Auto ranking/manifest integration.

**Physical validation (2026-09-02), against a real, independently-provisioned isolated peer on the Stockholm gateway (never the managed peer/identity) - full detail, including the blocker found and fixed along the way, kept here rather than summarized away:**

First physical attempt connected (`Protected`, `EVER_VALIDATED`) with only the four AWG obfuscation magic headers (H1-H4) configured, matching the LIVE Stockholm server's actual values (confirmed byte-for-byte via `awg showconf`, not the drifted `gateway/config/awg-profile.env` template). Despite that, and despite real UDP packets from the device physically reaching the gateway's `:51820` listener (confirmed via `tcpdump` on the server), the server never registered a handshake for the new peer (`awg show` showed no `latest handshake` line, ever) and the app's own "Protected"/`EVER_VALIDATED` state was a false positive - `ping` to the gateway's own tunnel address showed 100% loss, `curl` through the tunnel timed out, and after enough retries the app itself correctly, honestly transitioned to `Connection failed` / `Internet blocked until VPN reconnects` (a real fail-closed outcome, not manufactured for the negative test).

**Root cause**: `PrivateGatewayConfig`/`PrivateGatewayDialog` exposed only H1-H4 - never `Jc`/`Jmin`/`Jmax`/`S1`-`S4` (junk packet count and per-packet-type junk sizes), even though `AwgConfigMapper` already had full setters for all seven (shared with the managed `PocAwgProfile`, whose own doc comment already recorded that these ARE a real handshake blocker against this exact `gateway/provision.sh` build, not client-side-only cosmetic tuning - that comment's claim is what this physical test empirically confirmed). With no UI path to supply them, the client's AmneziaWG interface never enabled junk-packet obfuscation at all, so the live server (running `Jc=6/Jmin=40/Jmax=100/S1=113/S2=159`) never recognized the client's packets as valid AmneziaWG traffic.

**Fix**: extended `AwgProfile`'s existing seven junk fields (no new/second AWG config model) through `PrivateGatewayDialog` (7 new optional fields, clearly labeled, blank-by-default - never a hardcoded/invented value, since a different user's own VPS may need different values or none at all) -> `AppRoot`'s save handler (blank -> `null`, a non-blank non-numeric value fails closed with a new `INVALID_JUNK_PACKET_PARAMETERS` reason, never silently dropped) -> `PrivateGatewayConfigValidator` (new non-negative/min-max-not-inverted bounds check on the four now-wired fields, everything else unchanged - the four magic headers remain the only REQUIRED fields, preserving the existing test contract) -> `FilePrivateGatewayStore` (the four previously-unpersisted `*PacketJunkSize` keys added to read/write; a legacy file from before this fix - missing those keys entirely - reads back with them correctly `null`, never invented, proven by a dedicated regression test) -> the existing `AwgConfigMapper` setters, unchanged. 7 new/extended focused tests (full round-trip of all seven fields, legacy-file backward compatibility, validator bounds/inverted-range rejection, a full valid physical profile accepted, `AwgConfigMapper` now covers S3/S4 which the pre-existing test suite had left unexercised) plus the pre-existing 12 all still green - `compileDebugKotlin`/`assembleDebug` both pass.

**Re-validated physically end to end** on the same real device, against a FRESH isolated temporary peer (never the managed one) provisioned via the canonical `gateway/scripts/allocate-and-add-peer.sh`, with the client's Private Gateway config now carrying the full real live-server profile (`Jc=6/Jmin=40/Jmax=100/S1=113/S2=159/S3=0/S4=0` alongside the same H1-H4): a genuine handshake completed (`awg show` on the server: `latest handshake: 32 seconds ago`, real transfer counters climbing), real RX/TX confirmed both server-side (82.94 KiB received / 117.87 KiB sent server-side at the time of the check) and on-device (`EVER_VALIDATED`/`IS_VALIDATED`, real growing KB/s in the status bar), a real page fetch (`https://www.cloudflare.com/cdn-cgi/trace`) returned `ip=16.170.208.231`/`loc=SE` - conclusive proof traffic genuinely exits through the private Stockholm gateway, not the managed one - DNS resolved through the tunnel (`ValidatedPrivateDnsAddresses: [1.1.1.1, 1.0.0.1]` on `tun0`, no leak), and an IPv6-only fetch (`curl -6 https://ipv6.icanhazip.com`) timed out/failed closed despite the underlying WiFi network having real, working IPv6 connectivity - proving the private tunnel's `::/0` capture route, not a leak around it. PRIVATE -> AUTO was re-verified immediately after: real reconnect to Germany/Frankfurt with live throughput, and the managed Stockholm peer's own handshake/transfer counters were confirmed completely unchanged by the entire private-gateway test (same timestamp/byte-counts before and after). The temporary peer was removed via the canonical `remove-peer.sh` afterward; the managed peer was re-confirmed untouched one final time. No private key was ever printed, logged, or persisted at any point (confirmed via a full logcat scan across both physical passes: zero occurrences of any private key or full key material - even public keys appear in the app's own AmneziaWG debug logs only as truncated prefixes).

**Promoted to IMPLEMENTED** on this evidence: real handshake, real bidirectional data plane, real distinct exit IP, DNS/IPv6 invariants held, identity separation proven with public keys only, genuine (not manufactured) fail-closed behavior observed, and zero regression to the managed AUTO/MANUAL path. |
| Signed Offline Bootstrap | **IMPLEMENTED** (B17, 2026-09-01 - physically validated, no remaining caveat - see B17-2 below) | B11 built the real trust primitives: `EndpointManifest`/`ManifestCanonicalizer` (deterministic, dependency-free binary encoding), `Ed25519ManifestVerifier` (signature + expiry + clock-skew + rollback checks), `FileLastKnownGoodManifestStore` (durable, atomic-write LKG persistence surviving app restart), and a REAL cryptographically-signed `EmbeddedBootstrapManifest`. B12 adds the REAL distribution channel those primitives were missing: `GET /v1/manifest` (`gateway/api/handler.py`, unit- and integration-tested via `gateway/api/tests/test_manifest_endpoint.py`) serves an operator-deployed artifact verbatim (this process holds no signing key), and `HttpsRemoteManifestFetcher`/`ManifestDistributionClient` (Android) fetch and `offer()` it through the unchanged verification/rollback boundary. **B17 (2026-09-01) update**: the production key ceremony has now been PERFORMED - a real Ed25519 keypair was generated and used to sign entirely inside one local, offline script invocation (the private key was never printed to any tool/terminal output, never passed as a CLI argument, never committed, and is stored only in an operator-local file outside this repository; see `docs/B12_MANIFEST_KEY_CEREMONY.md`'s "Production ceremony (B17, completed)" section for the exact procedure and the public key's SHA-256 fingerprint). `EmbeddedBootstrapManifest` now embeds this REAL production public key and a REAL production-signed manifest (`manifestVersion=1`, signingKeyId `prod-manifest-key-2026-09-01`, 180-day expiry) naming BOTH real gateways - `frankfurt` (Germany/Oracle Cloud) and `stockholm` (Sweden/AWS) - each with only immutable public routing facts (id/roles/region/provider/AMNEZIA_WG+XRAY_REALITY+TLS_TCP host:port bindings), zero per-device secrets/client tunnel IPs/Xray credentials. `AutoGatewaySelector`'s DISCOVERY runtime authority was also moved this same slice - see the `automatic gateway selection / failover` row below for the exact change. **B17 (2026-09-01) live deployment, completed**: `GET /v1/manifest` is now real and live on BOTH production gateways - `POCVPN_API_MANIFEST_PATH=/etc/pocvpn/endpoint-manifest.bin` set and `pocvpn-api` restarted on each host, an nginx `location = /v1/manifest` (GET-only, same edge shape as `/v1/activate`/`/v1/xray-profile`) added and reloaded on each. Externally verified from outside either VPS: HTTPS 200 on both, exact sha256 byte identity to `gateway/tools/endpoint-manifest-2026-09-01.bin` on both, a real Ed25519 verification of the fetched bytes against the production public key succeeds, a bit-flipped copy and a wrong-key check both correctly fail verification, and POST is rejected (403) at the nginx edge on both hosts - `/v1/activate`/`/v1/xray-profile` behavior confirmed unaffected on both. A real drift was found and fixed DURING this deployment: Frankfurt's deployed `gateway/api/handler.py`/`config.py` (a plain file copy, not a git checkout) predated this repo's own manifest support entirely - fixed by hand-grafting only the minimal `_PATH_MANIFEST` route/`manifest_path` config field onto Frankfurt's existing deployed files (originals backed up in place, `py_compile`-checked before install), not a wholesale redeploy of `gateway/api/*.py` against a live server already serving real activation traffic - see `gateway/DEPLOYMENT.md`'s new "Deploying the signed manifest (B17)" section for the exact procedure and the honest note that Frankfurt's control-plane code otherwise remains behind this repo's HEAD (a separate future slice). Stockholm's deployed copy already had manifest support (deployed more recently, B15) - only the artifact/config/nginx steps were needed there. **B17 (2026-09-01) continuation - both remaining blockers closed, promoted to IMPLEMENTED**: (1) `BuildConfig.MANIFEST_URL` is no longer blank - `android/app/build.gradle.kts` now defines `PRODUCTION_MANIFEST_URL = "https://152.70.43.1/v1/manifest"` (Frankfurt, the sole configured primary - Stockholm serves the identical signed artifact from the same production key, but this client does not yet support multi-origin manifest fetch/failover, a deliberate, documented scope boundary, not an oversight) as an explicit literal, wired into BOTH `debug` and `release` `buildTypes` blocks (a developer's own gitignored `gateway-dev.properties` `manifestUrl` can still override it locally); this reuses the EXISTING `HttpsRemoteManifestFetcher`/`ManifestDistributionClient`/`MainViewModel.Factory` wiring unchanged - no second fetch mechanism. (2) A real physical-device validation pass was performed on the same debug build (installed with `-r`, preserving existing provisioning): a clean app restart genuinely fetched `GET https://152.70.43.1/v1/manifest` over HTTPS (confirmed via a new purely-observational `MainViewModel.lastManifestRefreshOutcome`/"Last manifest refresh:" diagnostics line - B17 addition, never a decision input) and the fetch was correctly, verifiably REJECTED with reason "candidate version 1 is not newer than the currently trusted manifest" - proving the live bytes were actually retrieved, decoded, and Ed25519-verified (a bad signature would have produced a DIFFERENT rejection reason), and correctly refused as a replacement because the live manifest and the embedded bootstrap are, BY THE B17 CEREMONY'S OWN DESIGN, the exact same signed version (never inconsistent trust roots - see `docs/B12_MANIFEST_KEY_CEREMONY.md`). The diagnostics dialog confirmed `Manifest version: 1 (source=EMBEDDED_BOOTSTRAP)` with BOTH `frankfurt` and `stockholm` endpoints present (each with AMNEZIA_WG/XRAY_REALITY/TLS_TCP transports), a real ranked 6-candidate Auto list spanning both gateways, and a real Auto connect reaching `Protected`/Germany. The LKG/offline fallback boundary was then proven with a reversible, CLIENT-SIDE-ONLY fault (airplane mode on the device - no production VPS touched, no route altered): force-restarting the app under airplane mode produced `Last manifest refresh: rejected: network error: ConnectException` while the SAME verified embedded bootstrap remained trusted, the SAME 6-candidate Auto list remained available, restoring network and reconnecting in Auto reached `Protected`/Germany again, and the airplane-mode fault was restored immediately. **B17-2 (2026-09-01) continuation - the real populated-LKG path is now physically proven, closing the prior caveat**: a production manifest **version 2** was signed offline using the SAME existing production key (no rotation, no regeneration - fingerprint unchanged: `2c39eddd256115600e3008495ee52b95865ab7a525f102f2fe45aad17b614aa1`), same endpoint facts as v1 (Germany/Frankfurt, Sweden/Stockholm, unchanged hosts/ports), a strictly higher `manifestVersion=2`, and a fresh issued/expiry window, deliberately kept AHEAD of the embedded bootstrap's `manifestVersion=1` so a real device genuinely adopts it as LKG rather than rejecting it as "not newer". Deployed byte-identical to BOTH gateways via the same established `/v1/manifest` path (file swap only - `pocvpn-api` reads the manifest path per-request, no restart needed, no code/config/nginx change), externally verified (HTTPS 200, exact sha256 match on both hosts, valid Ed25519 signature, `/v1/activate`/`/v1/xray-profile` unaffected). Physical pass on the same real device: a clean restart genuinely fetched the live v2 manifest and it was genuinely ACCEPTED (`Last manifest refresh: accepted version 2`, `Manifest version: 2 (source=LAST_KNOWN_GOOD)` - a REAL populated LKG, not the embedded bootstrap); both endpoints/a real ranked 6-candidate Auto list present with REACHABLE evidence; a real Auto connect reached `Protected`/Germany. Under a reversible, client-side-only airplane-mode fault (no production VPS touched, no route altered), force-restarting the app produced `Last manifest refresh: rejected: network error: ConnectException` while `Manifest version: 2 (source=LAST_KNOWN_GOOD)` and the full 6-candidate list SURVIVED the restart AND the fetch failure - the exact "remote accepted -> LKG persisted -> restart -> remote unavailable -> LKG used" sequence the B17 promotion rule requires. Restoring network and reconnecting in Auto reached `Protected`/Germany again; the airplane-mode fault was restored immediately. The "no LKG exists, embedded v1 bootstrap accepted" migration case remains proven only at the unit level (`EmbeddedBootstrapManifestTest`, `EndpointManifestRepositoryTest`) - accepted as sufficient for that case since a real device with a genuinely populated v2 LKG can no longer exercise an empty-LKG state without destructively wiping real provisioning. Stockholm's public IPv4 remains non-durable (unrelated, pre-existing limitation - see Gateway Pool row) and Frankfurt's broader control-plane code drift (found during B17's live deployment) remains a separate, deliberately unfixed future slice. **B20 (2026-09-01) - multi-origin manifest fetch, promoted, NOT elevating any HA/durability claim beyond what was physically proven**: Frankfurt is no longer the sole configured manifest-distribution origin. `BuildConfig.MANIFEST_URLS` (comma-separated, `ManifestOriginConfig.parse` validates/deduplicates/rejects malformed entries) now defaults to BOTH real production origins - Frankfurt `https://152.70.43.1/v1/manifest` and Stockholm `https://16.170.208.231/v1/manifest` - in both `debug`/`release` builds; a developer's gitignored `gateway-dev.properties` can override with the new `manifestUrls=url1,url2` or the pre-existing single-URL `manifestUrl=...` (kept working). No new VPS/resource was created - both origins are the SAME two production gateways this row already documents, confirmed via read-only SSH to still serve the byte-identical (sha256-matching) production-signed artifact (`2643ff6b...`) at B20 time, so **no server-side code change was needed or made**. New `MultiOriginManifestDistributionClient` (`android/app/src/main/java/net/pocvpn/client/reachability/ManifestOrigin.kt`) tries every configured origin, in order, on EVERY refresh (never stops early on the first accepted origin) - each origin's candidate goes through the SAME `EndpointManifestRepository.offer` signature/expiry/rollback boundary the single-origin `ManifestDistributionClient` always used (reused internally per origin, not reimplemented) - an origin is transport availability, NEVER a trust authority; a hostile/misconfigured origin serving modified bytes fails verification exactly like any other invalid candidate. Because `offer()` re-checks rollback against whatever is CURRENTLY trusted at each call, always trying every origin is what makes "highest valid version wins" correct: proven at the unit level - Frankfurt-valid-v2-then-Stockholm-valid-v3 converges on v3, and the reverse order leaves v3 trusted with v2 correctly a no-op. **PR #34 follow-up fix (2026-09-01) - typed classification, not string parsing**: `ManifestFetchResult.Failed` carries a typed `ManifestFetchFailureKind` (`NETWORK_ERROR`/`TLS_ERROR`/`HTTP_ERROR`/`MALFORMED`), and `ManifestVerificationResult.Invalid`/`ManifestUpdateResult.Rejected` each carry a typed `ManifestVerificationFailureKind`/`ManifestUpdateRejectionKind` (`UNKNOWN_SIGNING_KEY`/`CLOCK_SKEW`/`EXPIRED`/`INVALID_SIGNATURE`, plus `ROLLBACK_OR_NOT_NEWER` added by `EndpointManifestRepository.offer` itself) - set at the exact point each result is produced (`Ed25519ManifestVerifier`, `EndpointManifestRepository.offer`, `HttpsRemoteManifestFetcher`), never inferred afterwards from `reason`/`detail` text. `ManifestOriginOutcomeKind` (`NETWORK_ERROR`/`TLS_ERROR`/`HTTP_ERROR`/`MALFORMED`/`UNKNOWN_SIGNING_KEY`/`CLOCK_SKEW`/`INVALID_SIGNATURE`/`EXPIRED`/`ROLLBACK_OR_NOT_NEWER`/`ACCEPTED`) is a presentation-layer AGGREGATION of those typed categories via two small EXHAUSTIVE `when` mappings (`ManifestFetchFailureKind.toOriginOutcomeKind`/`ManifestUpdateRejectionKind.toOriginOutcomeKind` in `ManifestOrigin.kt`) - a compile error, not a silent misclassification, if either underlying enum ever gains a category this one doesn't cover yet; an unknown signing key is a distinct `UNKNOWN_SIGNING_KEY` outcome and is never misreported as `INVALID_SIGNATURE`. `ManifestOriginConfig` was additionally hardened to reject an HTTPS URL carrying userInfo (`user:password@host`), enforcing "no credentials in URLs" at the parser boundary. 5 new unit tests added for this fix (unknown-signing-key distinct from invalid-signature, clock-skew classification, exhaustive fetch-failure-kind mapping, exhaustive update-rejection-kind mapping, userInfo rejection) - full suite green (904/904). Diagnostics now show concise per-origin evidence plus the final result (`Last manifest refresh: 152.70.43.1=...; 16.170.208.231=... | final: ...`) - never a secret/credential field. `MainViewModel`'s ONE logical startup refresh and `manifestRefreshMutex` bounded-concurrency behavior are completely unchanged; control-plane isolation is unchanged/hard by construction (`ManifestOrigin.kt` references neither `EndpointReachability` nor `TransportHealth` nor `PathScorer` - manifest-origin failure cannot feed B19 path eligibility). A new debug-only "Refresh manifest" button (Diagnostics dialog) calls the EXACT same production `refreshManifest()` path for deterministic manual physical validation - no parallel test client. 18 new unit tests total (13 from the original B20 pass + 5 from the PR #34 typed-classification follow-up, `MultiOriginManifestDistributionClientTest`) cover: primary success, primary network/TLS/HTTP/signature failure each falling through to an accepted secondary, both version-race orderings, duplicate-URL dedup, malformed/blank entry rejection, all-origins-fail-preserves-LKG, all-origins-fail-with-no-LKG-uses-embedded-bootstrap, an invalid candidate never poisoning LKG, an expired candidate classified correctly, and zero-origins-configured. Full suite green (904/904) after the change; `compileDebugKotlin`/`compileReleaseKotlin`/`assembleDebug` all pass. **Physically verified on the same real device**: baseline - both origins genuinely attempted every refresh (startup and the new manual button), trusted manifest stayed v2/LKG, all 6 Auto candidates present, Auto connect reached Protected/Frankfurt. Fault injection - reversible, CLIENT-SIDE-ONLY (a debug-build `gateway-dev.properties` override pointing ONLY the Frankfurt origin slot at an unreachable `127.0.0.1:1`; no production server touched, no route/DNS/airplane-mode change, so - unlike B17-2's airplane-mode fault - the rest of the device's internet and the VPN data plane were completely unaffected throughout): Frankfurt origin genuinely failed (`127.0.0.1=NETWORK_ERROR`), Stockholm was genuinely still attempted and its real signed artifact verified through the SAME trust boundary (`16.170.208.231=ROLLBACK_OR_NOT_NEWER`, i.e. valid-but-not-newer-than-v2, proving verification succeeded), LKG/v2 remained trusted throughout, Auto candidates stayed fully populated, and a real Auto connect still reached Protected/Frankfurt - direct physical proof of the control-plane/data-plane isolation this row's manifest architecture has always claimed. Restoring `gateway-dev.properties` and rebuilding confirmed both origins reachable again; app left in a clean disconnected state. The bad-origin-crypto-candidate and highest-version-wins scenarios are proven at the unit level only, per this slice's own explicit scope (a safe physical mechanism to make a production origin serve a differently-versioned or tampered-but-still-HTTPS artifact would require touching a production server or standing up new infrastructure, both out of scope for B20). **Honest limitation, not overclaimed**: this does NOT provide quorum consensus, multi-provider control-plane HA, or stable Stockholm addressing - Stockholm's public IPv4 remains a non-durable AWS auto-assigned address (unchanged, see Gateway Pool row); redundancy improves ONLY for as long as that address stays valid, which is a separate operator/infrastructure decision requiring approval, not something this slice changed. |
| Alternative Control Routing | PLANNED | No code. |
| automatic gateway selection / failover | **IMPLEMENTED** (B16, 2026-09-01 - physically validated; B17/B17-2, 2026-09-01 - discovery AND execution-time endpoint address runtime authority moved to the signed manifest) | **B17-2 update (2026-09-01)**: the B17 discovery fix below still had a gap - the executed `GatewayConfigSnapshot`'s `endpointHost`/`endpointPort` were still read from `ProductionGatewayCatalog` (`gateway.awg.endpointHost/endpointPort`), so a signed manifest could gate WHICH endpoints exist but a rotated host/port in that manifest would silently NOT reach the real AWG attempt. Fixed: `buildCandidates` now carries the EXACT `EndpointTransportBinding` each candidate was scored from (keyed alongside the catalog descriptor in an internal `Triple`), and `snapshotFor(gateway, binding, clientTunnelIp)` reads `endpointHost`/`endpointPort` from that `binding` - never from the catalog - for every candidate, AWG included. Server public key/gateway tunnel IP/obfuscation profile remain catalog-sourced (the manifest model doesn't carry them yet - a real, stated scope boundary). Proven by new tests with a manifest host deliberately DIFFERENT from the catalog's: `AutoGatewaySelectorTest`'s `configSnapshot host and port come from the manifest binding, not the catalog, even when they differ`, and `MainViewModelAutoGatewayTest`'s `auto connect to Germany dials the manifest host, not the catalog host` (asserts the REAL executed `AwgConfig.peer.endpointHost`/`endpointPort`, not just the candidate's snapshot). **Xray/TLS execution address audited and left honestly unmoved**: `VpnController.buildTransportConfig`'s XRAY_REALITY/TLS_TCP branches resolve the connect-time server address from the endpoint-scoped `XrayProfileRepository`/`XrayTlsProfileRepository` (real control-plane-activation-populated), never from `GatewayConfigSnapshot` - that snapshot is consumed only by the AWG path. An Xray/TLS candidate's snapshot is still built (every candidate carries one) and is now manifest-derived too for consistency, but changing it has no effect on where an Xray/TLS attempt actually connects; no Xray profile-ownership migration was performed to force this scope, per the task's own explicit instruction not to. **B17 update**: `AutoGatewaySelector.buildCandidates` no longer takes a raw `gateways: List<ProductionGatewayDescriptor>` - it takes `manifestEndpoints: List<EndpointDescriptor>`, which `MainViewModel.buildAutoGatewayCandidates()` sources from `manifestRepository?.trusted()?.endpoints` (the verified `TrustedManifestState`, empty/fail-closed when nothing verifies or `manifestRepository` isn't wired - never a fallback to the catalog). `ProductionGatewayCatalog` is now consulted only through a `gatewayFactsFor: (EndpointId) -> ProductionGatewayDescriptor?` compatibility lookup, invoked only for an endpoint id the manifest already named, to supply the AWG connection facts (server key, tunnel IP, obfuscation profile) that deliberately stay out of the public manifest. Local per-device provisioning (`ClientTunnelIdentityStore`, Xray/TLS profile availability) still gates which of the manifest's declared transports this device can actually use - a manifest naming an endpoint never implies local provisioning for it. Proven by new tests: an endpoint absent from the trusted manifest is never a candidate even though it exists in the catalog; no trusted manifest source yields zero candidates; both endpoints become candidates exactly when manifest + local provisioning both agree (`AutoGatewaySelectorTest`, `MainViewModelAutoGatewayTest`). The B16 candidate-identity/pinning/bounded-failover invariants below are otherwise completely unchanged - this is a discovery-source change only, not a rescoring or re-pinning change. Real, pure decision logic exists and is live-wired for real connect() attempts when `MainViewModel.gatewayAutoMode` is true (device-local, persisted, default `false`/Manual - every pre-B16 install stays Manual-only, byte-for-byte unaffected). `AutoGatewaySelector` (`smartconnect/AutoGatewaySelector.kt`) promotes the EXISTING `ReachabilityEngine`/`PathCandidateBuilder`/`PathScorer` pipeline (reused verbatim - see PathScorer's own "OBSERVATIONAL ONLY" note, now genuinely promoted for this one call site, not reimplemented) into a ranked `GatewayAttemptCandidate` list across every gateway this device is actually PROVISIONED for (`ClientTunnelIdentityStore`-backed, same readiness check the manual picker already uses - an unprovisioned gateway is never a candidate). `EndpointDescriptor`s for ranking come from `ProductionGatewayEndpoints`, built directly from `ProductionGatewayCatalog` - deliberately NOT the Signed Offline Bootstrap manifest (still only names "frankfurt"; extending it needs the offline manifest-signing key ceremony, explicitly out of scope for this slice). Candidate identity (`gatewayId`/`endpointId`/`transport`/`GatewayConfigSnapshot`) is resolved ONCE per candidate at ranking time and threaded verbatim into execution: `AutoGatewaySelector`'s `GatewayAttemptCandidate.configSnapshot` is carried through `TransportOrchestrator.resolve(..., gatewayConfigSnapshot = candidate.configSnapshot)` into `TransportOrchestrator.Resolution.Resolved.gatewayConfigSnapshot`, and `VpnController.connect()` pins it (`pendingConnectConfig`) for the ENTIRE attempt (including across a VPN-permission-prompt round-trip) - `doConnectAttempt()`/`gatewayStatus()` both resolve through this SAME pinned value (via the shared `GatewayConfigSnapshotValidator`, reused unchanged from `DefaultGatewayConfigurationRepository`) whenever it is set, and `gatewayConfigurationRepository`/`SelectedGatewayStore`/`ProductionGatewayCatalog`/`ClientTunnelIdentityStore` are NEVER re-consulted once a candidate is pinned - proven by `VpnControllerPinnedGatewayConfigTest` (mutating the repository/identity store after pinning, including across the permission-resume gap, provably cannot change what gets executed) and `MainViewModelAutoGatewayTest`. Manual gateway selection (`selectGateway()`) is completely unaffected (its `Resolution.Resolved` never carries a snapshot, so `doConnectAttempt`/`gatewayStatus()` fall through to `gatewayConfigurationRepository` exactly as every pre-B16 call site did), remains deterministic, and turns Auto back off when used. On a genuine terminal failure (`AutoGatewayFailoverPolicy` - the SAME enumerated HandshakeTimeout/BackendStartFailure categories `AwgXrayFailoverPolicy` already uses, never a generic catch-all), the existing `armFailoverWatch` collector advances to the next ranked candidate, bounded (`MAX_ATTEMPTS=4`, never retrying an already-attempted (gateway, transport) pair) and fails closed (`VpnError.NoCandidateAvailable`) once the ranked set is exhausted - never an unbounded loop, never a random pick. UNKNOWN reachability evidence is never treated as UNREACHABLE (PathScorer's own reachability tier already ranks UNKNOWN above DEGRADED/UNREACHABLE, unchanged); with no evidence at all, ties resolve deterministically via `PathScorer.rank`'s existing tiebreak (`SmartConnectDecisionEngine.PREFERRED_ORDER` then candidate id) - in practice AMNEZIA_WG on Germany before Stockholm, never random. **Failover model, stated precisely (consolidated review fix)**: `armFailoverWatch()` branches on whether the retained attempt carries an `autoContext` BEFORE it ever reaches the `AwgXrayFailoverPolicy` check - so during an Auto sequence, `AwgXrayFailoverPolicy` is NOT consulted at all; advancement follows the ONE globally-ranked gateway×transport candidate list instead (the preferred B16 model - a failed AWG candidate on one gateway can be followed by ANY higher-ranked remaining candidate, including that SAME gateway's own XRAY_REALITY/TLS_TCP or the OTHER gateway's AMNEZIA_WG, purely by rank, never a hardcoded "try Xray on this gateway first" chain). `AwgXrayFailoverPolicy`'s own intra-gateway AWG->Xray fallback continues to govern, completely unchanged, ONLY for Manual mode (`autoContext == null`) - it does not additionally run "within" an Auto sequence's current gateway the way an earlier draft of this row incorrectly claimed. Compact diagnostics (`MainViewModel.autoGatewayDiagnostics`/`.autoGatewayCandidates()`, surfaced in the debug diagnostics dialog) show the ranked candidate list, current/attempted candidates, last failure category, and the actual active gateway - never a secret/credential field. UI: `GatewayPickerDialog` gained a leading "Auto" row (task's minimal-UI requirement); Home shows the ACTUAL active gateway once Auto has targeted one, an "Auto / Smart Connect" placeholder only while idle and nothing has been attempted yet. **B16 (2026-09-01) physical validation, completed**: run on the same real physical Android device (ADB-connected, debug build, real production Germany/Stockholm gateways) prior B10-B15 evidence came from. Real flow: Auto enabled (persisted, `gateway_auto_mode.txt`); `GatewayPickerDialog` confirmed BOTH Germany and Stockholm show as provisioned rows; a real `connect()` produced a ranked 6-candidate list (`GERMANY/AMNEZIA_WG` and `STOCKHOLM/AMNEZIA_WG` genuinely tied at score 3031968, deterministic tie broken by candidate id - Germany first, exactly as `PathScorer.rank`'s own tiebreak predicts) and connected to Germany - confirmed via a real AWG keepalive in logcat matching Germany's exact real peer public key (`9WewKC/zyUPyPnKyzaI0bZrEN2c73PqjK7f+fRXHYRU=`). A controlled, fully reversible, non-destructive fault was then induced ON-DEVICE ONLY (no server/infrastructure touched): this device's own `ClientTunnelIdentityStore` entry for Germany was removed via `run-as` (the debug build's own debuggable-app shell access), making Germany genuinely unprovisioned on this device - the SAME real "no client tunnel identity" condition `isGatewayProvisioned`/`AutoGatewaySelector`'s `provisioned` filter already handle in production, never a fabricated/mocked failure. A real `connect()` then ranked Stockholm-only, connected, reported `Protected`, and the diagnostics dialog's OWN `Gateway:` line (the live gateway-status output actually driving the tunnel) read `16.170.208.231:51820` - Stockholm's real production endpoint. (At the time of this physical pass, that line was driven by the since-superseded `ActiveAttemptGatewaySource` override described in an earlier draft of this row; a subsequent consolidated review fix replaced that mechanism with the pinned-`GatewayConfigSnapshot` execution boundary this row now documents above, which `VpnControllerPinnedGatewayConfigTest` proves directly at the unit level - the observed physical behavior/evidence itself is unaffected, only the underlying wiring producing it was hardened.) The device-local fault was restored immediately (Germany's identity entry rewritten back), and a fresh disconnect/reconnect in the SAME still-enabled Auto mode returned to Germany, confirming normal operation resumed with no lingering state. Auto's own persistence survived a full APK reinstall (stronger than a plain app restart). **One real bug was found and fixed by this pass**: the pre-existing debug diagnostics `Gateway:` line (`AppRoot.kt`'s `smartConnectGatewayLine`) always recomputed a MANUAL-mode-shaped decision (`smartConnectDecision()`, keyed off the persisted manual selection) regardless of `gatewayAutoMode` - while genuinely connected to Stockholm via Auto, this ONE diagnostics line still read "Germany / Frankfurt" (confirmed on-device before the fix). Fixed to source from `MainViewModel.activeGatewayId` while Auto is engaged (same "actually running, never a hypothetical recompute" discipline `currentTransportLine` already documented) - re-verified on-device with the same induced-failure scenario, now correctly reads "Sweden / Stockholm"; the separately-added `Active gateway:` line was already correct throughout and was never affected. No production selection/failover/scoring logic was touched by this fix - UI diagnostics string composition only; full unit suite re-run green (804/804) after the fix. **Caveat, honestly scoped**: this physical pass exercised the "unprovisioned gateway excluded from the candidate set, Auto connects the other real gateway" path end-to-end for real; it did NOT exercise a genuine mid-connect() AWG handshake-timeout triggering `AutoGatewayFailoverPolicy`'s SAME-request retry-to-next-candidate advance on this physical device (that would require either root - unavailable on this device - or a reversible server-side fault the operator would need to perform via SSH, out of scope for this pass) - that specific code path remains proven only by `MainViewModelAutoGatewayTest`'s controlled-fake-transport unit tests, which exercise the identical `AutoGatewayFailoverPolicy`/`armFailoverWatch` logic this row's selection mechanism already shares. 41 unit tests total (`AutoGatewaySelectorTest`, `AutoGatewayFailoverPolicyTest`, `ProductionGatewayEndpointsTest`, `MainViewModelAutoGatewayTest`, `VpnControllerPinnedGatewayConfigTest`) covering: both gateways considered, unprovisioned exclusion, manual Germany/Stockholm never cross-fail, bounded two-candidate failover with a controlled fake transport, fail-closed exhaustion, immediate top-candidate success, PathHistory influencing ranking, mode persistence across a simulated restart, and (consolidated review fix) the pinned-candidate-config immutability guarantee - proven both at the VpnController level (repository/permission-resume mutation cannot change an in-flight attempt) and at the MainViewModel level (mutating `ClientTunnelIdentityStore` across a real permission-resume gap cannot change the executed AWG config). |

## P1

| Capability | Status | Notes |
|---|---|---|
| Connection Memory | **IMPLEMENTED** (recording AND, for Automatic gateway selection, decision-driving - physically verified 2026-09-01, see caveat) | `ConnectionOutcomeStore`/`FileConnectionOutcomeStore` (B8I) are real: durable (`connection_outcomes.bin`, survives app restart), bounded, tested, and already wired into every real connect attempt (`VpnController.recordConnectionOutcome`) - "did AWG/Frankfurt tend to work, and how fast," exactly this row's own description. B11 adds a genuinely NETWORK-SPECIFIC counterpart: `FilePathHistoryStore`, keyed `networkFingerprint x endpointId x transportKind` (an app-local, per-install HMAC fingerprint over coarse network signals - see Endpoint / Path Reachability Fabric's own privacy note), durable and bounded the same way, and B12 proves per-endpoint separation holds under a real multi-endpoint manifest (`GatewayPoolIntegrationTest` - success recorded for endpoint A never credits endpoint B). B12 also derives real per-endpoint DATA-PLANE evidence (`reachabilityDiagnostics()`'s `endpointSpecificReachable`) straight from `ConnectionOutcomeStore`'s own `gatewayId`/`transport` fields - real evidence, no new recording path, no change to `VpnController`. **B13 - `FilePathHistoryStore.record()` is now genuinely wired into the real connect path**: `VpnController.recordPathHistory()` (additive, no-op unless `pathHistoryStore`/`fingerprintKeyProvider`/`networkProfileProvider` are all wired - production always wires them, via the SAME instances `reachabilityDiagnostics()` already reads) is called from the exact same authoritative-outcome sites `recordConnectionOutcome` already uses - one write per real completed attempt (AWG success, AWG handshake timeout, any-kind backend-start failure, one write for a whole exhausted-reconnect cycle) - never for a merely "Connecting" state, never a fabricated candidate, never twice for the same attempt (`VpnControllerPathHistoryTest`). The endpoint this writes against is now the REAL `SmartConnectCandidateSelector`-chosen `GatewayCandidate.id`, threaded through `TransportOrchestrator.Resolution.Resolved.endpointId` and `VpnController.pendingConnectEndpointId` - never a hardcoded `ProductionGateway.ID` literal at the write site (it merely still defaults to it, since only one real endpoint exists). Kept at IMPLEMENTED for the recording mechanism, and **B19 (2026-09-01) makes it genuinely decision-driving for Auto mode too**: `PathHistoryEntry` gained a `consecutiveFailures` field (the RECENT streak - resets to 0 on any success, distinct from the lifetime `failureCount` that never decreases), and `PathScorer.score` now derives a bounded, time-decaying cooldown penalty from it (capped, naturally expiring after `PathScorer.FAILURE_COOLDOWN_WINDOW_MILLIS` with no separate cleanup mechanism, cleared immediately by the next success) - never a permanent blacklist, and never persisted anywhere but the SAME `FilePathHistoryStore` file (format version bumped 1->2; a pre-B19 file simply falls back to empty, the same fail-safe every other store here already uses). `PathScorerTest`'s own cooldown cases prove the bound, the expiry, and the success-clears-it behavior directly. **Physically verified end to end (2026-09-01)**: two real debug-injected `FAILURE` outcomes for Frankfurt AWG produced a live `FAILURE_COOLDOWN`/`RECENT_FAILURE_THIS_NETWORK`-tagged, score=-1080 candidate in the real Auto plan; a subsequent real-shaped `SUCCESS` write (then a genuinely real AWG connection) replaced those tokens with `RECENT_SUCCESS_THIS_NETWORK` and a score of 3030975 - the bounded penalty, its typed reasons, and its clearing-on-success all confirmed on a real device, not merely at the unit level (see Gateway Health / Reachability row for the full evidence trail - same physical validation pass). `SmartConnectDecisionEngine`/Manual mode remains unaffected (see Smart Connect row) - B14's "not yet decision-making" caveat now applies ONLY to that path, not to Auto gateway selection. |
| Transport Scoring | **FOUNDATION** | `TransportScorer` (B8N) is real and unit-tested: a deterministic score combining each transport's real `TransportHealth` (B8L1, dominant signal - a NOT_IMPLEMENTED transport always scores lowest) with its declared `TransportCapabilities.maturity` (tie-break only). Live-wired: `MainViewModel.transportScores()` computes it from the real registry/health on every read and surfaces it in the diagnostics UI. Kept at FOUNDATION, not IMPLEMENTED: deliberately NOT passed into `smartConnectDecision()` - same "real evidence, truthfully surfaced, not yet decision-driving" boundary as `RestrictionClassifier`/`TransportHealth` - `SmartConnectDecisionEngine` still picks by its own fixed `PREFERRED_ORDER`, not this score. |
| Emergency Gateway Rotation | PLANNED | No code; requires Gateway Pool first. |
| Shadowsocks fallback | FOUNDATION (selection-wired, not yet selectable) | Stale row - see the B45 Transport Diversity Expansion row above for the real, current status. Real code exists (types, encrypted endpoint-scoped credentials, adapter shell, B45B-4 selection wiring into TransportRegistry/AutoGatewaySelector/SmartConnectDecisionEngine/VpnController) but no production `libsslocal.so` is packaged yet, so it stays fail-closed ineligible on every real device today. |
| multi-hop | PLANNED | No code. |
| shared exit pools | PLANNED | No code. |
| RAM-only / diskless gateway infrastructure | PLANNED | No code; today's gateway provisioning writes to disk (`/etc/amnezia/amneziawg/`), by design, for the local-test POC. |
| Traffic Pattern Defense / DAITA-like defense | PLANNED | No code. |
| Traffic Adaptation / DPI Resilience | PLANNED | No code. Purpose: let Nova choose a safe, transport-specific traffic adaptation profile when real evidence suggests DPI/protocol interference, rather than only choosing WHICH transport/endpoint to use (that decision stays `ReachabilityEngine`/`PathScorer`'s job - B11, FOUNDATION). Future decision key: `networkFingerprint x endpointId x transportKind x adaptationProfile -> observed connection outcome` - the SAME `networkFingerprint x endpointId x transportKind` key `FilePathHistoryStore` (B11, Connection Memory row above) already uses today, extended with one more dimension rather than a new, parallel memory model. Safe future adaptation candidates: `NONE` (always the default), TLS handshake/write fragmentation, bounded transport padding, safe transport-level segmentation. Architecture rules for whenever this is actually built: adaptation is always transport-specific (never a global packet mangler shared across transports), `NONE` is always the default and every other profile requires real reachability/failure evidence before ever being selected, outcomes integrate with local Connection/Path Memory and a future `PathScorer` extension (never a separate, disconnected scoring path), retry/strategy rotation must be bounded (no unbounded profile-cycling loop), and no third-party service is ever impersonated (same architecture principle 4 discipline as Hard Whitelist Detection). This does NOT solve a destination-IP hard whitelist (`POSSIBLE_HARD_WHITELIST` - see `RestrictionClassifier`) - DPI/protocol-signature adaptation and a fixed-destination allowlist are different failure modes with different remediations, and must not be conflated. Explicitly NOT adopted from zapret2 or any similar tool, now or later: no root/NFQUEUE dependency, no fake packet injection, no bad-checksum tricks, no TTL manipulation, no TCP sequence/ACK deception, no arbitrary OS-level packet reordering, and zapret2 itself is never embedded or copied into Nova. |
| automatic tunnel-key rotation | PLANNED | No code. Client identity today (B4) is a single long-lived AndroidKeyStore-backed keypair. |
| per-device keys and revocation | PLANNED | No code. Today's identity model is single-device, non-revocable. |
| DNS tracker/malware protection | PLANNED | No code. DNS itself is out of scope until B10. |
| REALITY Port 443 & SNI/ServerName Rotation Research | RESEARCH | **B35 (2026-09-05).** Separate optimization/research branch, explicitly not the hard-whitelist solution and not a blocker for CHAIN_CDN (see P0 CDN rows). Research scope: 2053 vs 443 for direct REALITY, impact on existing nginx/control-plane ownership of TCP/443 (today's `/v1/manifest`/`/v1/activate` edge, B15/B20/B26/B31), safe REALITY `serverName` rotation, compatibility with the pinned `gateway/xray/VERSION` core, handshake/stability/data-plane testing. Constraints: must not break control-plane HTTPS, must never claim port 443 or SNI rotation alone solves IP whitelist restrictions, no third-party impersonation (architecture principle 4). No code exists yet. |
| Capacity / Load Validation | PLANNED | **B35.** Before any scale claim, the project must measure its OWN active sessions, request/connection rate, throughput, p95 latency, error rate, CDN/provider limits, origin CPU/RAM, nginx connection pressure, Xray connection/resource pressure, and cost where relevant - never copy community capacity claims into product truth (extends architecture principle 10's truthfulness commitment to capacity specifically). No measurement tooling or data exists yet. |

## P2 - advanced

| Capability | Status | Notes |
|---|---|---|
| hybrid post-quantum protection | PLANNED | No code. |
| rotating exit IP | PLANNED | No code; requires Gateway Pool. |
| Tor-over-VPN where appropriate | PLANNED | No code. |
| custom DNS / DoH / DoT | PLANNED | No code. |
| self-hosted/private VPS mode improvements | PLANNED | Builds on Private Gateway Mode (PLANNED). |
| quotas / abuse protection | PLANNED | No code. |
| advanced routing controls | PLANNED | Builds on `ClientRoutingPolicy` (FOUNDATION). |
| BlancVPN Xray Extra Research | RESEARCH / UNVERIFIED | **B35 (2026-09-05).** Recorded explicitly as research, not architecture fact: BlancVPN publicly exposes "Xray"/"Xray Extra" product modes for Russia; whether "Extra" means REALITY, XHTTP, CDN, gRPC, multi-hop, dynamic SNI, specialized fingerprinting, or specialized routing profiles - or something else - is UNKNOWN and must not be promoted into architecture fact without evidence. Future findings are to be evaluated for architectural principles only - never copy or reuse another provider's private configurations, credentials, UUIDs, private keys, subscription secrets, or provider secrets (consistent with this repository's existing "never hardcode a named third party's infrastructure" rule, architecture principle 4). No investigation performed by this entry. |

## NACF — Nova Adaptive Censorship Fabric

**Approved resilience program.** B35, B36 and B37 are merged implementations; this does not itself prove new production topology. B35 has physical **unrestricted-network** Android -> CDN -> Stockholm XHTTP ingress -> Frankfurt EXIT -> Internet proof. B36 and B37 diagnostics are **PARTIAL**. Real Russia/restricted-network/hard-whitelist resilience remains **UNVERIFIED**.

Binding principles:

- No single transport, endpoint, ASN, provider, CDN, or control-plane dependency. Country and provider names never decide routing.
- `END_TO_END_DATA_PLANE_OK` outranks DNS/TCP/TLS-only reachability. Unknown capabilities fail closed; probes are bounded. Failures remain typed by DNS, TLS, transport, ingress, relay, exit, and data-plane layer, without exception-string guessing.
- Routine infrastructure rotation uses existing signed configuration where safe instead of requiring APK releases. Reuse existing trust, activation, and relay models; introduce no new cryptography.
- Extend `NetworkProfiler`, `RestrictionClassifier`, `ReachabilityEngine`, `PathCandidateBuilder`, `PathScorer`, `AutoGatewaySelector`, `TransportHealth`, `RoutingDecisionEngine`, the relay model, and B29 diagnostics. Create no parallel connection or diagnostics authority.
- Never impersonate unrelated third-party services or use unauthorized residential infrastructure. No Russia/provider hardcoding. Russia and hard-whitelist claims require real field evidence.

| Slice | Status | Scope and evidence gate |
|---|---|---|
| B37 Typed Runtime Failure Signals | **PARTIAL** | The existing out-of-band relay-health HTTPS probe now carries closed DNS-resolution, TLS-handshake and request-timeout facts from its actual `IOException` types into B29/B36 as `RELAY_PROBE_DNS_FAILURE`, `RELAY_PROBE_TLS_FAILURE` and `RELAY_PROBE_TIMEOUT` for a pinned relayed attempt. These describe the probe request, not the CDN hop or in-tunnel data plane; its existing relay failure category and connection/failover behavior remain unchanged. For Xray-native relays the optional probe is observational and cannot veto an already connected tunnel; its typed result is an event, not a fabricated terminal failure. Existing XHTTP handshake, out-of-band relay proof, native remote confirmation and relay watchdog mappings remain. Xray's opaque core result does not distinguish CDN DNS/TLS/HTTP status, and Android cannot observe the CDN-to-origin or origin-proxy hop; CDN_DNS_FAILURE, CDN_TLS_FAILURE, CDN_METHOD_REJECTED, CDN_RATE_LIMITED, CDN_TIMEOUT, CDN_ORIGIN_UNREACHABLE, ORIGIN_TLS_FAILURE and ORIGIN_PROXY_FAILURE remain intentionally un-emitted. No exception-message inference, extra probes, or new diagnostics authority. Russia/restricted-network behavior remains UNVERIFIED. |
| B36/B37 R3 Runtime Truth Audit | **COMPLETE (CODE/UNIT), PHYSICAL UNVERIFIED** | The reason-by-reason owner, structured signal, path/stage constraint, runtime-emittability, unit evidence, and remaining instrumentation gap are recorded in [`R3_B36_B37_FAILURE_TRUTH_TABLE.md`](R3_B36_B37_FAILURE_TRUTH_TABLE.md). The audit found no truthful missing production mapping: five CDN causes remain reserved pending typed XHTTP/core evidence, while three origin-stage causes cannot be known by the current Android architecture. Regression tests prove all eight are unreachable from current structured mapping inputs, generic HTTP status and generic exceptions stay generic, and diagnostics do not change Auto failover eligibility or sequence. No physical typed failure was exercised. |
| B38 Multi-Provider / Multi-ASN Path Diversity | **PARTIAL** | Existing signed endpoint-binding metadata can carry bounded opaque operator, network, region, CDN and control-plane failure-domain IDs. B38 derives complete-path correlation from the actual pinned direct or ingress+exit hops and uses a capped final Auto-ranking tie-break only when a fully-known path has a domain unique among alternatives. Missing/malformed/legacy metadata earns no independence preference; path IDs, manual selection, health, reachability, provisioning and route compatibility remain unchanged. The present production topology has two documented infrastructure operators (Stockholm ingress and Frankfurt exit) and one CDN family in the verified XHTTP chain, but no B38-signed production diversity metadata or additional independently executable provider route has been deployed. Architecture support is implemented; production provider expansion and field survivability proof remain pending. Russia/restricted-network behavior remains UNVERIFIED. |
| B39 Network-Specific Path Memory | **PARTIAL** | Extends the existing persistent `PathHistoryStore` keyed by opaque `NetworkFingerprinter` output × logical `historyPathId` × transport. Real authoritative connection/relay outcomes already feed the existing scorer and Auto selector; B39 adds an explicit typed key boundary, invalid-key fail-neutral handling, bounded saturating counters, and existing deterministic max-entry eviction. No destination, host, IP, SSID/BSSID, credential, telemetry, or new probe is stored. Current health/reachability and manual selection remain authoritative; Russia/restricted-network behavior remains UNVERIFIED. |
| B40 Restriction Classifier v2 | **PARTIAL** | Adds a closed qualitative `RestrictionAssessment` over the existing single classifier: bounded evidence-quality, contradiction, staleness and completeness reasons without probabilities, new restriction classes, probes, or routing rules. Existing conservative `RestrictionClass` output and precedence remain authoritative; Russia/restricted-network/hard-whitelist field behavior remains UNVERIFIED. |
| B41 NACF Path Synthesizer | **PARTIAL** | Extends the existing candidate-construction authority with deterministic endpoint/transport enumeration, duplicate candidate suppression and a hard synthesis bound while preserving explicit trusted `relayTo` route composition. PathScorer and AutoGatewaySelector remain the scoring/selection authorities; profile/provisioning coverage remains in existing execution boundaries. No arbitrary ingress×exit cross-product, new transport, probe, infrastructure or provider/country logic. Russia/restricted-network/hard-whitelist behavior remains UNVERIFIED. |
| B42 Signed Transport Profiles | **FOUNDATION** | Adds a typed, endpoint-bound boundary over existing signed transport metadata: legacy bindings remain compatible, CDN/XHTTP profiles use the existing versioned semantic validator, unsupported or invalid profiles fail closed, and no credential-bearing material enters the manifest. The new abstraction is not yet consumed by live candidate construction or execution; existing manifest canonicalization/signature, local profile repositories, route authority, and attempt pinning remain authoritative. Russia/restricted-network behavior remains UNVERIFIED. |
| B43 Bootstrap Resilience | **PARTIAL** | The live startup path now uses the single verified manifest repository with deterministic LIVE/origin refresh, atomic monotonic LKG persistence, rollback rejection, bounded HTTPS payload/timeouts, corruption-neutral loading, verified embedded-bootstrap fallback, and monotonic fencing for stale refresh callbacks. Existing typed fetch/validation outcomes remain separate from trust decisions; concurrent refreshes are serialized and B41 consumes only the repository's accepted manifest. Broader independently operated control-plane source diversity and wider connection/session fencing remain future work. Russia/restricted-network behavior remains UNVERIFIED. |
| B44 Emergency Infrastructure Rotation | **PARTIAL** | Adds signed endpoint operational state (`ACTIVE`, `DISABLED`, `RETIRED`) using existing manifest metadata/canonicalization. Disabled or retired endpoints are rejected structurally before candidate scoring, legacy missing state remains ACTIVE, unknown/conflicting state fails closed, and replacement topology is supplied only by a newer trusted manifest with existing local credential/provisioning gates. Route-level disable, active-tunnel teardown, and production control-plane emission remain out of scope. Russia/restricted-network behavior remains UNVERIFIED. |
| B45 Transport Diversity Expansion | B45A COMPLETE — FEASIBILITY PROVEN. B45B-1/B45B-2/B45B-3 MERGED — isolated production adapter shell exists, unreachable from selection. B45B-3P — production adapter physically validated in a debug carrier. **B45B release packaging: OPTION A — PRODUCTION PACKAGING APPROVED** (release-only `jniLibs.useLegacyPackaging`, physically proven end to end - see this row's own notes). **B45B-4 MERGED (selection wiring only, NOT a selectability claim)**: `TransportKind.SHADOWSOCKS_2022` is now wired into `MainViewModel.buildTransportRegistry`/`AutoGatewaySelector`/`SmartConnectDecisionEngine.PREFERRED_ORDER` (appended last, no reordering)/`VpnController.supportedKinds`+`buildTransportConfig`, gated fail-closed on THREE narrow facts: a wired endpoint-scoped `Shadowsocks2022CredentialRepository` with a genuinely valid credential, AND device ABI limited to `arm64-v8a` (`ShadowsocksAdapterEligibilityChecker`), AND a resolved, executable `libsslocal.so` at that ABI. No production `libsslocal.so` is packaged in `main`/`release` as of B45B-4 (only a differently-named debug-only spike binary exists) - SHADOWSOCKS_2022 therefore stays genuinely ineligible/unselectable on every real checkout today, by design, until a later slice adds real production packaging for that binary. No physical selection/runtime proof, no Wi-Fi/cellular handover proof, and no Russia/hard-whitelist proof exist for this wiring - none are claimed. | B45A (independent `shadowsocks-rust` AEAD-2022 runtime, never routed through Xray's own module) is now physically proven on real Android hardware: real `VpnService` TUN ownership, real `SCM_RIGHTS` TUN-fd handoff, real `protect(fd)`, real end-to-end TCP AND UDP data-plane traffic (3/3 exact UDP round-trip proofs), and deterministic lifecycle - see [`B45A_SHADOWSOCKS_RUST_SPIKE.md`](B45A_SHADOWSOCKS_RUST_SPIKE.md) for the full round-by-round evidence trail. Network handover (Wi-Fi/cellular) remains explicitly UNVERIFIED/deferred. A production-adapter design (reusing `VpnTransport`/`TransportOrchestrator`/Smart Connect/B42-signed-profile/existing credential-storage authorities rather than a second architecture) is written up in [`B45B_SHADOWSOCKS_PRODUCTION_ADAPTER_DESIGN.md`](B45B_SHADOWSOCKS_PRODUCTION_ADAPTER_DESIGN.md), with a proposed slice breakdown (B45B-1 through B45B-5+). B45B-1 (types/profile/capabilities), B45B-2 (encrypted endpoint-scoped credential repository), and B45B-3 (isolated `ShadowsocksTransport`/`ShadowsocksVpnService`/`ShadowsocksRuntime`/bridges production adapter shell) are merged into main - `TransportKind.SHADOWSOCKS_2022` stays `NOT_IMPLEMENTED` in `TransportRegistry`, no live factory, unreachable from Smart Connect/`TransportOrchestrator`/normal selection. B45B-3P physically validated that production adapter shell end to end on a real device using a reproducible locally-built `sslocal` binary packaged debug-only (never committed, never release-packaged): real `SCM_RIGHTS` TUN-fd handoff with a corrected borrow-only `FileDescriptor` ownership design (a first attempt using `ParcelFileDescriptor.adoptFd` crashed the process via an fdsan ownership conflict with the VpnService-owned TUN descriptor - fixed), real TCP and UDP (3/3 exact byte matches, server-side decrypt/relay/re-encrypt corroborated) data-plane proof, 120s stability, and a normal-stop Unix-domain-socket cleanup bug found and fixed - see [`B45B3P_SHADOWSOCKS_PHYSICAL_VALIDATION.md`](B45B3P_SHADOWSOCKS_PHYSICAL_VALIDATION.md) for the full evidence trail. Release-safe native packaging for this binary, `TransportRegistry` availability, Smart Connect/`TransportOrchestrator` wiring, Q7 handover, and Russia/hard-whitelist behavior all remain unresolved/unverified - B45 is NOT complete. Routing Shadowsocks through Xray's own module remains REJECT FOR NOW as a diversity claim, kept strictly separate from the row above. OpenVPN 2.6 `tls-crypt-v2` remains RESEARCH CONTINUES - real protocol feature, OpenVPN3 core's license is now identified (dual AGPLv3/MPL-2.0, MPL-2.0's file-level copyleft a plausible path) but which path VPNrus's actual JNI/linkage shape falls under needs FORMAL LEGAL REVIEW, `ics-openvpn` (GPLv2) and tls-crypt-v2 Android-UI-completeness remain unresolved, and tls-crypt-v2 does not resist DPI fingerprinting by itself. Plain WireGuard/IKEv2 remain non-priority. No new cryptography researched or proposed. Russia/restricted-network field evidence for either candidate remains UNVERIFIED and is not claimed.

**B45B release packaging - OPTION A APPROVED (2026-09-18).** A bounded architecture spike compared four candidate mechanisms for shipping the pinned `sslocal` executable in the release APK without an app-writable executable file: Option B (extract-and-chmod to app-private storage) and Option G (anonymous sealed `memfd` + `execveat`/`execve` via `/proc/self/fd`) were both physically REJECTED on the real OPPO test device - `EACCES` on every execution attempt, with Option G's failure reproduced identically via two independent syscall entry points, converging on the same platform-level restriction Option B already hit. Option D (an install-time dynamic-feature module isolating just `libsslocal.so`) was REJECTED for a distribution-model reason, not a packaging one: local/non-Play `bundletool`/`installDebug` device-targeted delivery never actually installed the feature-module split (tested across two bundletool versions and every documented CLI flag), and a universal/single-APK build was shown to fuse the module's content back into the base APK's own native-lib tree, destroying the isolation the option exists to provide. **Option A (`android.packaging.jniLibs.useLegacyPackaging = true`, applied release-only via the same Variant API shape the debug variant already used) is the one mechanism physically proven to work end to end** - see `android/app/build.gradle.kts`'s own `androidComponents.onVariants(selector().withName("release"))` block. It is module-wide by construction (no per-file AGP granularity exists for this setting - confirmed, not assumed): Xray's `libgojni.so` and AWG's `libwg*.so` are legacy/extracted in release exactly like `libsslocal.so`, a known and now-measured cost, not an oversight.

*Physical proof, real OPPO device, release-packaged build (2026-09-18):* AWG - real `Protected` state, real handshake (`AmneziaWG/pocvpn: Received handshake response`), real external exit `152.70.43.1` (`icanhazip.com` through the tunnel), clean disconnect, no crash/ANR. Xray - `libgojni.so` loaded from the legacy-extracted `nativeLibraryDir` with no `UnsatisfiedLinkError` (`nativeloader: Load .../lib/arm64/libgojni.so ... ok`), real `Xray 26.7.28` core start (`NovaXrayVpnService: Xray core started`), real `Protected` state reached via the existing debug-only "Force XRAY_REALITY" Diagnostics control (never a production selection change), Diagnostics itself confirming `Current transport: XRAY_REALITY` (ruling out an AWG-fallback false positive), real external exit `152.70.43.1`, clean disconnect (`NovaXrayVpnService: tearing down: explicit stop`), no crash/ANR. Production UI unchanged throughout (screenshots taken at every stage).

*Measured packaging cost, same release build, before/after `useLegacyPackaging`:* APK size `173,619,611` -> `75,401,956` bytes (-56.5%); installed on-device footprint (real `codePath` + extracted `lib/` on the OPPO) `~166 MiB` -> `~112 MiB` (-32.5%). Both figures went down, not up: modern/uncompressed packaging's mandatory 16 KiB page-alignment padding for this app's specific native-lib set (large, numerous `.so` files across 4 ABIs) costs more than the legacy-packaging duplicate extracted copy it avoids. Direct APK/sideload distribution is unaffected either way - this is a single-APK packaging attribute, not a split/bundle mechanism.

*Provenance.* The pinned upstream `sslocal` (shadowsocks-rust v1.25.0, commit `ab388c7466d21f979430e33cc9ef10e22fb05955`) has SHA-256 `333eafee26e5e7fdad91892bdb7400ad397e7d7f99d42f26b37c8ca92bc2c1f6` - this is the value to verify the binary against BEFORE placing it at `android/app/src/main/jniLibs/arm64-v8a/libsslocal.so` (gitignored, never committed, same discipline as every other pinned binary in this repo). AGP's own `stripReleaseDebugSymbols` task successfully strips this specific binary's ELF section-header table during release packaging (unlike the debug-variant harness's own strip attempt, which has always failed silently on it and left it byte-identical) - the byte actually shipped inside the signed release APK therefore has a DIFFERENT hash, SHA-256 `9b17a4b707b0ae2d54e4ef14a1d72985e651afc4e4f0e88b6d3664a93cea03de`. This is a reproducible, deterministic build-time transformation, not a supply-chain mismatch or tampering - both hashes are recorded here explicitly so a future SHA verification step checks the correct one for the artifact it is actually inspecting (upstream pinned value pre-build, or this post-strip value against the shipped APK).

*Server-side incident, found and fixed during Xray physical regression (2026-09-18, Frankfurt production gateway).* `POST /v1/xray-profile` returned HTTP 500 (`PermissionError`) for every request, blocking `XrayProfileProvisioningOutcome.Saved` and therefore `XRAY_REALITY` availability for any device, independent of any client-side change. Root-caused (read-only audit, then two narrowly-scoped fixes, no service restart) to two stray file-ownership defects predating this work: `/var/lib/pocvpn-xray/xray-activation.last-sha256` and `/etc/pocvpn/relay/static-relay-clients.json`, both left `root:root` by an earlier operator action instead of the `pocvpn-api` service's own identity. Both corrected to `pocvpn-api:pocvpn-api`, mode `600` unchanged (no permissions widened); `pocvpn-api`, `nova-xray`, `awg-firewall`, and `nginx` all confirmed `active` throughout, no restart required. No activation credential, profile secret, or private key is recorded here or was ever logged.

*ABI status - explicit, unresolved.* The pinned `sslocal` binary is physically proven for `arm64-v8a` only. Before `SHADOWSOCKS_2022` can become generally selectable, one of two things is required: (A) build and package `sslocal` for every ABI this app currently ships (`armeabi-v7a`/`x86`/`x86_64`, same cross-compile pattern already used for the AWG/Xray third-party build scripts), or (B) an explicit, fail-closed ABI eligibility gate in `TransportRegistry` (checked against `Build.SUPPORTED_ABIS`) so a non-`arm64-v8a` device never sees `SHADOWSOCKS_2022` reported available. Neither exists yet - this row records the requirement, it does not implement it. `TransportRegistry`/Smart Connect selection wiring for `SHADOWSOCKS_2022` itself is entirely unstarted (B45B-4+); this packaging approval unblocks that future work, it does not itself change what is selectable today. |
| B46 QUIC / HTTP3 Research Track | RESEARCH - **B46-2B: TECHNICALLY READY - LICENSING DECISION REQUIRED before any physical/release integration; Android physical proof pending regardless** | Test reachability, blocking, fingerprint, mobile stability, and full data-plane proof before any runtime commitment. **B46-1 (2026-09-18)**: candidate/architecture research complete - see [`B46_QUIC_HTTP3_RESEARCH.md`](B46_QUIC_HTTP3_RESEARCH.md). Reconciled against prior OPEN/UNMERGED PR #35 (B21 XHTTP-h3 spike): Xray XHTTP-h3 is real in the pinned core but does not deliver independent-failure-domain transport diversity; no candidate has Nova-specific Android physical-feasibility evidence yet. **B46-2A (2026-09-18, corrected same day against current upstream)**: Hysteria2 Android feasibility PREPARATION - see [`B46_2A_HYSTERIA2_ANDROID_FEASIBILITY.md`](B46_2A_HYSTERIA2_ANDROID_FEASIBILITY.md). Re-audited against current stable `app/v2.12.3` (commit `e1366b173ccf5706e1e4630fe8aa654a4b574085`, MIT). Structurally-equivalent arm64-v8a artifact built (NDK r26c, `CGO_ENABLED=1`, `-checklinkname=0` needed to work around a new `wlynxg/anet` build regression) matching the official release artifact's toolchain/CGO/dependency shape but not yet byte-identical to it (own two builds ARE byte-identical to each other). Verdict: RESEARCH BLOCKED - Hysteria2's shipped `tun` client mode cannot accept a VpnService-created TUN fd, so a TUN-relay layer (reusing `sing-tun`, already vendored by Hysteria2 itself) must be designed before any physical device test. **B46-2B (2026-09-18, corrected same day following direct PR review)**: TUN bridge architecture spike - see [`B46_2B_HYSTERIA_TUN_BRIDGE_ARCHITECTURE.md`](B46_2B_HYSTERIA_TUN_BRIDGE_ARCHITECTURE.md). Re-confirmed the same B46-2A TUN-fd gap directly against a fresh clone of the identical `app/v2.12.3` commit (no upstream change). Compared three architectures - Nova-side `sing-tun` adapter feeding Hysteria2's own unmodified SOCKS5 listener; a minimal Hysteria2 fd-injection patch; a third-party tun2socks bridge (this last option now backed by a REAL audit of two current candidates, `xjasonlyu/tun2socks` and `heiher/hev-socks5-tunnel`, not a structural dismissal) - and selected the Nova-side `sing-tun` adapter (`NOVA_SING_TUN_ADAPTER`), in-process with the `VpnService` (the process boundary is now explicitly PINNED, matching Nova's own existing Xray-core JNI precedent, not left open). **TUN fd ownership is corrected to a deterministic split-ownership model**: the original `ParcelFileDescriptor`'s fd is never handed to `sing-tun` directly (its own `Close()` would close that exact fd number, risking a double-close with the `VpnService`'s own close) - a DUPLICATE (`dup()`+`detachFd()`) is created and transferred exclusively to the bridge, so no component ever believes it owns the same close responsibility as another. **The host-side synthetic proof was corrected and re-run**: it now duplicates the TUN fd before handing anything to `sing-tun`, proves a REAL FULL UDP ROUND TRIP (not merely flow demux) with deterministic channel-synchronized verification of both the observed metadata and the returned payload, and was run under `go build -race`, which found zero races in the proof's own code but surfaced one genuine, previously-unknown internal data race inside the then-pinned `apernet/sing-tun` dependency itself (`TCPNat.LookupBack`, writing `session.LastActive` outside its own lock). The debug-only B46 state machine's bridge-failure transition was corrected (renamed `bridgeExitedUnexpectedly`/`BridgeExited` to `bridgeFailed`/`BridgeFailed`) so a bridge failure no longer falsely clears a still-owned Hysteria2 runtime pid - only an actual confirmed runtime-process exit may do that; a direct regression test proves this. `TUN_BRIDGE_READY`'s definition was tightened to state exactly what it does and does not mean. **B46-2B dependency correction (2026-09-18, same day, third pass)**: the race above is now RESOLVED, not merely reported - the Nova bridge's OWN `sing-tun` pin (independent of Hysteria2's, safe because the two never share a process/build, per the now-pinned in-process boundary) was moved to current `github.com/sagernet/sing-tun@fbc0c3dff312e91f512756ad843af74dd209577c`, which fixes the race structurally upstream (per-session locking, verified by reading source). The proof was ported to that dependency's changed API (its `NewConnectionEx`/`NewPacketConnectionEx` handler methods no longer force-close the connection after returning - a genuine improvement; `WritePacket` requires pre-reserved buffer headroom or it panics - both discovered by running the port, not read speculatively) and re-proven: real TCP round trip, real full UDP round trip, deterministic, and **race-clean under `go build -race`, twice in a row (zero reports)**. A real `GOOS=android GOARCH=arm64 CGO_ENABLED=0` build of the same program succeeds with no NDK and no linkname workaround - stronger Android build-level evidence than Hysteria2's own binary needed. Hysteria2 itself is untouched, still pinned to `apernet/sing-tun`. `v2.12.1`'s server-side QUIC-stateless-reset change still only partially/unconfirmed-ly addresses idle/screen-lock issue `#1510`, which remains open - still a mandatory future physical test case. **B46-2B licensing correction (2026-09-18, same day, fourth pass) - the most consequential correction in this row's history.** A direct license audit found that BOTH `sing-tun` forks this row references (`apernet/sing-tun` and the bridge's own `sagernet/sing-tun` pin) are **GPL-3.0-or-later, NOT MIT** as every earlier pass incorrectly stated (verified directly from each fork's own `LICENSE` file; the error came from conflating Hysteria2's own genuinely-MIT top-level `LICENSE.md` with the separate `sing-tun` module). VPNrus's own repository has **no root `LICENSE` file** - no owner decision on GPL-compatible distribution exists, and this task does not make one. The prior pass's Android-build claim is also corrected: `GOOS=android GOARCH=arm64 CGO_ENABLED=0 go build` only proved an executable cross-build, NOT the real `Kotlin/VpnService -> JNI -> in-process Go bridge` boundary. This pass built the REAL artifact instead: a `gomobile bind`-produced Android `.aar` (Android SDK + NDK installed for this check) whose native library was confirmed, via `go tool nm`, to contain 1,344 linked `sing-tun` symbols - real evidence the GPL code genuinely links in-process into what would load into Nova's `VpnService`. Separately, Hysteria2's own child-process binary was independently confirmed (fresh build + `go tool nm`, 125 linked symbols) to also contain GPL-3.0-or-later `sing-tun` code regardless of whether Nova ever enables Hysteria2's own `tun:` config - a second, parallel licensing question. A deep audit of `heiher/hev-socks5-tunnel` (MIT, all-permissive dependency tree, a purpose-built TUN->SOCKS5 daemon with real external-fd support and correct non-owning close semantics, real Android JNI/AAR precedent added by upstream one day before this audit, and a real native `arm64-v8a` `.so` built successfully via NDK in this pass) found no blocker but its own live TCP/UDP round-trip proof was not completed in this already-long pass. Verdict, re-decided again rather than preserved by default: **TECHNICALLY READY - LICENSING DECISION REQUIRED** for the `sing-tun` path; B46-2P must not proceed as a release architecture on GPL-3.0-or-later `sing-tun` until the repository owner makes an explicit, informed licensing decision. A follow-up completing `hev-socks5-tunnel`'s round-trip proof could independently reach a clean ready verdict on an all-MIT path with no such decision required. No repository license was added or changed by this pass. No production code/dependency/config change in this pass. |
| B47 Traffic Fingerprint Audit | PLANNED | Audit owned path fingerprints and protocol compatibility; do not impersonate unrelated third-party services. |
| B48 Active-Probing Resistance Audit | PLANNED | Audit ingress responses, authentication boundaries, replay behavior, and exposure using owned/authorized infrastructure only. |
| B49 Censorship / Chaos Simulation Framework | PLANNED | Reproduce bounded DNS/TLS/transport/ingress/relay/exit faults and verify typed failover, proof, and fail-closed behavior; simulation is not field proof. |
| B50 Provider/ASN Survivability Score | PLANNED | Score measured path diversity and reliability without provider-name allowlists or claims of universal reachability. |
| B51 Owned/authorized Non-Datacenter Endpoint Research | RESEARCH | Evaluate legal, operator-authorized endpoint diversity only; no unauthorized residential infrastructure. |
| B52 Offline / Outage Mode research | RESEARCH | Define safe last-known-good, explicit unavailable states, and outage UX without bypassing manifest trust. |
| B53 Android Device Exposure Hardening | PLANNED | Audit Android VPN lifecycle, local secret boundaries, logs, and support export against device-side exposure. |
| B54 Restricted-Network Field Validation Matrix | PLANNED | Record real operator/network/device/time evidence, including Russia/hard-whitelist cases; publish only measured end-to-end outcomes, never infer bypass from reachability. |
| B55 Continuous Anti-Censorship Intelligence Loop | PLANNED | Feed authorized field findings and typed diagnostics back into bounded tests, scoring, and signed rotation decisions with reviewable evidence. |

## Current verification status (not features - claims about the running system)

| Claim | Status |
|---|---|
| Local Android<->WSL2 AmneziaWG handshake | **VERIFIED** (B8A) |
| Public-network AWG handshake | **VERIFIED** (B10-1, 2026-08-30) - real physical Android device (ADB-connected, `net.pocvpn.client` debug build), real mobile/Wi-Fi network, real gateway `152.70.43.1:51820`; logcat: `Sending handshake initiation` -> `Received handshake response`; `dumpsys`/`ip addr` confirm `GoBackend$VpnService` running with `tun0 10.77.0.5/32`. |
| Public VPS NAT behavior | **VERIFIED** (B10-1, 2026-08-30) - tunnel exit IP (`152.70.43.1`, the gateway's own address) confirmed via two independent third-party services (icanhazip.com, dnsleaktest.com) from a real device browser tab, differing from the device's real baseline IP. |
| External public IP change through the tunnel | **VERIFIED** (B10-1, 2026-08-30) - baseline `86.49.236.33` (icanhazip.com, disconnected) -> connected `152.70.43.1` (icanhazip.com AND dnsleaktest.com independently agree), all via real browser traffic, not shell-UID traffic. See `docs/B10_LEAK_VALIDATION_PLAN.md`'s Results section for the full evidence. |
| DNS leak protection | **VERIFIED** (B10-1, 2026-08-30) - dnsleaktest.com Standard Test: exactly 1 resolver found, Cloudflare (`172.71.140.49`, Frankfurt am Main), zero trace of the device's real carrier DNS (`62.141.16.181`/`.151`, confirmed present via `dumpsys connectivity` while disconnected). |
| IPv4/IPv6 leak protection | **VERIFIED** (B10-1, 2026-08-30) - IPv4: exit IP matched the gateway only, in two independent real-browser checks, never the device's real ISP IPv4. IPv6: a real-ISP IPv6 baseline was confirmed to exist first (`2a02:8308:...`), then while connected an IPv6-only hostname failed to resolve (`ERR_NAME_NOT_RESOLVED`) AND a raw IPv6 literal (no DNS involved) timed out (`ERR_CONNECTION_TIMED_OUT`) - the device's real ISP IPv6 never reached any external destination through either path. |
| Real restrictive-network/Russia behavior | **UNVERIFIED** - restriction-classification and bounded probing foundations exist (`NetworkProfiler`, `RestrictionClassifier`, `ReachabilityEngine`, B28), but real Russia/restricted-network/hard-whitelist behavior has no field proof. |

Do not cite any UNVERIFIED row above as if it were proven. Each becomes
VERIFIED only when its corresponding gate (B8B/B9/B10) produces real
on-the-wire evidence, the same way B8A did for the local handshake and
B10-1 did for the five rows above.
